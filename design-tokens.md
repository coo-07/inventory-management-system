# 在庫管理システム デザイン方針メモ

Claude Designでのモックアップ確認を踏まえて決定した、実装時のデザインルール。
VS Code + Claude Codeでの実装時にこのまま渡してOK。

---

## 1. 「戻る」系リンクの文字

現状(小さめ・薄いグレー)から視認性を上げる。

```jsx
// Before
<button className="text-sm text-gray-400">← 一覧へ戻る</button>

// After
<button className="text-base font-medium text-gray-700 flex items-center gap-1 py-2">
  <ArrowLeftIcon className="w-5 h-5" />
  一覧へ戻る
</button>
```

- `text-sm`(14px) → `text-base`(16px)
- `text-gray-400` → `text-gray-700`
- 矢印アイコンを追加して「戻る」感を直感的に

対象箇所：一覧へ戻る／商品詳細へ戻る／戻る（登録・編集画面）

**30番で更新：** この「戻る」系リンクを含むヘッダー部分は、画面上部にsticky固定される仕様に変更（30番参照）。

---

## 2. 商品画像プレースホルダー（カテゴリ別アイコン・色分け）

**アイコン方式：絵文字ベースで確定**（Tabler等のアイコンライブラリは導入しない。カラフルで直感的、追加ライブラリ不要で実装もシンプルなため）

画像未設定時は、カテゴリごとに絵文字アイコン＋背景色を表示。
`utils/categoryStyle.js` として共通化し、一覧・詳細どちらでも使い回す。

```js
// utils/categoryStyle.js
export const categoryStyles = {
  "文房具": { icon: "✏️", bg: "bg-blue-50" },
  "雑貨":   { icon: "🎁", bg: "bg-purple-50" },
  "食品":   { icon: "🍙", bg: "bg-orange-50" },
  "default": { icon: "📦", bg: "bg-gray-50" },
};
```

表示優先順位：商品画像（アップロード済みならそれを優先）＞ カテゴリアイコン

**アイコンサイズ：縦横30px**（トップページの商品ボックス内アイコン）

```jsx
<span className="w-[30px] h-[30px] flex items-center justify-center text-[24px]">
  {icon}
</span>
```

---

## 3. カテゴリ文字の視認性

```jsx
// Before
<p className="text-xs text-gray-400">{category}</p>

// After
<p className="text-sm text-gray-600 font-medium">{category}</p>
```

- `text-xs`(12px) → `text-sm`(14px)
- `text-gray-400` → `text-gray-600`

---

## 4. カテゴリ入力：プルダウン化（案A採用）

**案A（今回のMVP採用）：** 固定カテゴリ＋自由入力のプルダウン
- 選択肢例：「文房具」「雑貨」「食品」「その他（自由入力）」
- 入力ミス防止・実装がシンプル・「入力項目を減らす」方針とも合う

**案B（将来追加予定に位置づけ）：** 業種テンプレート選択（雑貨店／飲食店／アパレルなど）
- 初回設定で業種を選ぶと、その業種向けカテゴリ候補が自動表示
- データモデルに「業種」概念の追加が必要 → 今回のMVPスコープ外
- 企画書12章「将来追加予定」に追記：「業種別カテゴリテンプレートへの対応」

```jsx
// 実装イメージ
const categoryOptions = ["文房具", "雑貨", "食品", "その他"];

<select className="...">
  {categoryOptions.map(opt => <option key={opt}>{opt}</option>)}
</select>
// 「その他」選択時のみテキスト入力欄を表示
```

---

## 5. ボタンのタップ領域

```jsx
// Before
<button className="px-3 py-1.5 text-sm">保存する</button>

// After
<button className="px-6 py-3 text-base min-h-[44px]">保存する</button>
```

- `min-h-[44px]` を全ボタン共通ルールにする（Appleのタップ領域推奨サイズ）
- 対象：保存する／キャンセル／編集する／削除する／入出荷を記録／新しい商品を登録　など全ボタン

---

## 6. カテゴリアイコンの粒度（大分類で統一）

細かく分けすぎず、大まかな括りで統一する。

| アイコン | カテゴリ | カバー範囲 |
|---|---|---|
| 🐾 | ペット用品 | 猫・犬・鳥・小動物などのフード・グッズ全般 |
| ☕ | 飲み物 | コーヒー・紅茶・お茶・ジュースなど |
| ✏️ | 文房具 | (既存) |
| 🎁 | 雑貨 | (既存) |
| 🍙 | 食品 | 飲み物以外の食料品 |
| 👕 | 衣類 | 服・アパレル系 |
| 🧴 | 美容・コスメ | 化粧品・スキンケアなど |
| 📦 | その他 | どれにも当てはまらないもの |

「その他」で新規カテゴリを追加する際は、上記のような大分類アイコンの候補から選ぶ方式（絵文字ピッカー）にする。

---

## 7. カテゴリ絵文字のサイズ（追記）

一覧画面で確認したところ、カテゴリ文字（✏️ 文房具／🎁 雑貨など）の絵文字が文字とほぼ同サイズで埋もれ気味だったため、絵文字部分だけ拡大する。

```jsx
// Before
<span className="text-sm">✏️ 文房具</span>

// After（絵文字だけ大きめに）
<span className="inline-flex items-center gap-1">
  <span className="text-base">✏️</span>
  <span className="text-sm text-gray-600 font-medium">文房具</span>
</span>
```

- 絵文字サイズの目安：現在の1.3〜1.5倍程度
- カテゴリ文字（3番）とのバランスを見ながら調整。絵文字がアイコンとして目立つ大きさにする

---

## 8. 店舗情報画面（軽量版・案A採用）

店名（例：田中商店）をクリックすると店舗情報画面に遷移する。

- 店舗名・住所・電話番号を編集できるシンプルな画面
- 権限管理・ログインの概念は含めない（現行のMVPスコープ）
- 「管理者が触れる」という権限の概念は、企画書12章「将来追加予定」に追記済み（9番の権限3層構造を参照）

---

## 9. 将来の権限構造（B型事業所展開・追記）

職業訓練校で同じくIT系を学んでいる、福祉系の職場（就労継続支援B型事業所）を手伝っている方への聞き取りをもとに、将来的な権限構造を以下のように想定する。実装は今回のMVPスコープ外（企画書12章にも反映済み）。

| 役割 | 主な操作 | 画面の特徴 |
|---|---|---|
| 管理者 | 全機能＋店舗情報編集＋権限管理 | 通常のフル機能画面 |
| スタッフ | 商品登録・編集・削除、入出荷記録、在庫確認 | 通常のフル機能画面（権限管理は不可） |
| 利用者（障害者） | 棚卸し（実際に数えた在庫数の記入）のみ | 棚卸し専用のシンプル画面 |

**利用者（障害者）向け「棚卸し専用画面」の設計方針**

- 実際のB型事業所では、利用者が商品の棚卸し（数を数えて記録する作業）を担うケースがあることを踏まえた設計
- 商品を1つずつ大きく表示（画像またはカテゴリ絵文字＋商品名）
- 現在庫数ではなく「実際に数えた数」を入力する専用の大きな入力欄（＋／−ボタンかテンキー風の数字パッドを想定）
- 一覧から選ぶのではなく、商品が1件ずつ順番に表示される「数える→入力→次へ」のシンプルな繰り返しフロー
- 入力後は「登録できました」といった明確な完了フィードバックを表示（他の項目とも共通の方針）

---

## 10. トップページ 商品カードの文字サイズ

**商品名テキストサイズ：21px**

```jsx
// Before
<p className="text-lg font-medium">{name}</p>

// After
<p className="text-[21px] font-medium">{name}</p>
```

**商品名が長い場合：折り返しではなく省略表示（`…`）にする**

自動縮小（文字サイズを小さくして1行に収める）は、12番・13-5の「文字サイズは常に固定」方針と矛盾し、カードごとに文字サイズがバラつく原因になるため採用しない。代わりに、はみ出た部分を`…`で省略し、文字サイズは常に21pxで統一する。

```jsx
<p 
  className="text-[21px] font-medium truncate"
  title={name}  // ホバーで全文表示（ツールチップ）
>
  {name}
</p>
```

- `truncate`（Tailwind）：`white-space: nowrap; overflow: hidden; text-overflow: ellipsis;`をまとめて指定
- 全文は`title`属性のホバー表示、または商品詳細画面（企画書6章の画面構成）で確認できる

---

## 11. アプリ名について

- **正式名称（企画書・プレゼン用）**：在庫管理システム
- **アプリ内表示名（UI上のブランド名）**：やさしい在庫管理
- 「やさしい在庫管理」という名前は、くう自身が明示的に指定したものではなく、Claude Designが企画書のコンセプト（「誰でも迷わず使える」「Excelより簡単、専門ツールより手軽」）を汲み取って自動的に付けた名称
- 分かりやすく親しみやすいため、そのまま採用することに決定
- プレゼン等で聞かれた場合の説明案：「システムとしての正式名称は『在庫管理システム』ですが、UIには親しみやすさを込めて『やさしい在庫管理』という名前を付けています」

---

## 12. アクセシビリティ・UI基本ルール（PC操作が苦手な人向け）

参考資料：[業務システムのUI設計・改善の勘所](https://ncdc.co.jp/columns/8296/)（NCDCコラム）を踏まえて追記。

### 12-1. UDフォント指定

```css
font-family: "BIZ UDGothic", "Hiragino Sans", "Yu Gothic", sans-serif;
```

- Windows → BIZ UDゴシック／Mac → ヒラギノ角ゴ（UD角ゴがなければヒラギノ角ゴで代用可）
- Webフォント読み込みが必要な場合はGoogle Fontsの「BIZ UDGothic」を利用可能

### 12-2. 色のルール

- 背景：白（`bg-white`）
- 基本文字色：`#333333`
- 意味を持つ色は最大5〜6色まで（例：赤＝警告・発注点未満、青＝リンク／操作可、緑＝完了、グレー＝非活性）
- 「装飾のためだけの色」は使わない
- 余白を広めに取り、視線誘導を意識する

### 12-3. クリック可能性の明示

5番（ボタンのタップ領域）とセットで運用。

```jsx
// ボタン共通
className="... hover:bg-blue-600 transition-colors border border-transparent hover:border-blue-700"
```

- ホバーで色が変わる／枠線や立体感を付けて「押せる」ことを示す

### 12-4. ボタン文言は具体的な動詞で統一

- 「編集」ではなく「編集する」、「削除」ではなく「削除する」のように統一
- 既存のdesign-tokens（5番）の「保存する」「キャンセル」などもこのルールに沿っている

### 12-5. 訓練校の先生からの補足（ビジュアル・アイコン優先の原則）

「ビジュアルを使う」「アイコンを使う」「できるだけ文字を減らす」という考え方は、訓練校の授業でも指導されている一般的なUI原則。今のdesign-tokensの以下の項目は、すでにこの原則に沿っている。

- カテゴリ絵文字アイコン（2番・6番・7番）
- 「戻る」への矢印アイコン追加（1番）
- 入力項目を最小限に絞る設計（企画書のコンセプト）

---

## 将来のアイデアメモ（このアプリ限定ではないが、将来検討の記録として残す）

- **ぐちゃぐちゃなデータの取り込み（訓練校の先生の話より）**：フォーマットが崩れたExcel/CSVでも、AI（Dify）が内容を解釈し、商品名・カテゴリ・在庫数などを自動で振り分けて取り込めるようにする、というアイデア。
  - 在庫管理システムの将来機能としては、企画書12章「Excel/CSVインポート」を発展させる形で反映可能
  - このアイデア自体は在庫管理システムに限らず、汎用的なDifyアプリ（「ぐちゃぐちゃデータ整形AI」）としても成立しうるため、ポートフォリオ全体の企画候補としてClaudeの記憶にも登録済み

---

## 13. レスポンシブ・省スペース対応ルール（ノートPC実機対応）

MacBook Air（13.6インチ）実機確認で、右端に上下スクロールが発生し見づらい問題が発覚したことへの対応。デスクトップの大画面基準で組んだUIを、ノートPC基準に見直す。

**対象範囲・目的：** 「レスポンシブ対応」ではなく、PC（ノートPC〜デスクトップ）の画面幅に絞った「ファーストビュー（FV）最適化」が目的。スクロールなしで一目で商品状況を把握できることを優先する。モバイル（スマホ・タブレット）対応は今回のスコープ外（発表日9/1のため、まずはPC版を完成させることを優先。時間が余れば着手）。

**実機の論理解像度（参考・検証済み）：** MacBook Air M2 13.6インチは、スケーリング設定により論理解像度が1,024×666／1,280×832／1,470×956（デフォルト）／1,710×1,112の4段階で変化する。くうの自宅PC（MacBook Air M2）はデフォルト設定なら1,470px、職業訓練校のデスクトップMacはこれより大きい画面。13-5のclamp()基準（1,280px〜1,920px）は、この実機の値を踏まえて設定している（「文字を大きく」設定などで1,280px未満になる場合は、後述の通りそれ以上縮小させない設計）。

### 13-1. 商品画像プレースホルダーの箱を「密度連動の高さ」にする（修正版）

**経緯：** 当初は正方形（`aspect-square`）→横長4:3（`aspect-[4/3]`）→高さ140px固定、と変遷したが、Claude Designの実装を確認したところ、実際には`var(--img-h)`というCSS変数で、グリッドの`--grid-min`と同じ「density（密度）トグル」に連動する方式になっていた。

| プリセット | `--img-h` |
|---|---|
| ゆったり（デフォルト） | 120px |
| コンパクト | 88px |

- カードの横幅（列数）とは無関係に決まるため、13-1で問題視していた「列が少ないと箱が巨大化する」現象は起きない
- `--grid-min`（13-2）と同じdensityシステムで一元管理されているため、設計として一貫性がある
- 当初の目安140pxに近い値（120px）になっており、この実装のままでOKと判断

```jsx
<div style={{height: "var(--img-h)"}} className="bg-blue-50 flex items-center justify-center overflow-hidden">
  <img src={imageUrl} className="w-full h-full object-cover" />
  <span className="w-[30px] h-[30px] flex items-center justify-center text-[24px]">{icon}</span>
</div>
```

### 13-2. グリッドを「カード最小幅＋自動列数」方式にする（auto-fit + 中央寄せに再修正）

**経緯：** 当初はTailwindのbreakpoint（`md:grid-cols-3 xl:grid-cols-4`）で列数を段階的に切り替える方式にしていたが、この方式だと画面がどれだけ広くても4列で頭打ちになる。商品数が多い場合、横3〜4列固定だと縦スクロールが増え、管理しづらいというフィードバックがあったため、**カードの最小幅だけ決めて、列数は画面幅に応じて自動で決まる**方式（CSS Gridの`auto-fill`）に変更した。

さらにその後、商品数が少ないテストデータ（6個）で画面幅が広い場合、`auto-fill`が「実際のカード数に関係なく、画面に入るだけの列の枠を確保する」仕様のため、右側に大きな空白ができる問題が見つかった。`auto-fit`＋`justify-content: center`＋カード幅の上限指定に修正する。

```css
.product-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 240px));
  justify-content: center;
  gap: 24px;
}
```

- `auto-fill` → `auto-fit`：使われていない空の列トラックをなくす
- `minmax(220px, 240px)`：カードの上限を240px程度に設定（上限がないと、商品数が少ない時にカードが際限なく巨大化するため）
- `justify-content: center`：商品の合計幅が画面より狭い場合、左寄せではなく中央配置にする（右側だけ空白になるのを防ぐ）
- カード1枚の基準サイズ：幅220〜240px・高さは中身（アイコン・商品名・在庫行・詳細ボタン）の積み上げで決まる（13-1のdensity連動`--img-h`は18-4で56×56pxアイコン方式に上書き済みのため、現在は不使用）
- 画面が狭ければ自動的に列数が減り、広ければ自動的に列数が増える。商品数が少ない場合は中央に寄って表示される
- 13-5（1280px〜1920pxの連続スケーリング方針）と矛盾しない
- カードは220px未満には縮まない（`minmax`の下限で保証）

**数値の変遷（2026/08/03 訓練校iMacでの実機確認を経て最終確定）：**

当初`minmax(240px, 280px)`で運用していたが、訓練校のiMac（24インチ・デフォルト論理解像度2240px）で実機確認したところ、17番のコンテナ最大幅（1800px）の制約もあり、横に並ぶ商品カードの列数が想定より少なく（6列程度）、「個人商店の商品数を考えると、パッと見て把握できる列数がもっと欲しい」という判断から、以下の2点を見直した。

1. コンテナの`max-width`を1800px→2400pxに拡張（17番を更新）
2. カード幅の上限を280px→240pxに縮小（本項目）

この2つの調整により、iMac（デフォルト論理解像度）で7列、「スペースを拡大」設定時で8列表示になることを確認。個人商店規模の商品数を一覧しやすいバランスとして採用を決定した。MacBook Air（ノートPC、1280〜1470px幅）では列数への影響はなく、7列表示は維持される。

**さらなる圧縮検証（2026/08/03、20番のページ件数動的算出との兼ね合いで着手・結果は未確定）：**

20番で1ページの表示件数を「列数×3行」で動的算出する方式にしたところ、3行表示でも縦スクロールがほぼ発生しない一方、「もっと一目に見せたい」「画面が大きくなってもカードは大きくなりすぎない方がいい」という方向性から、カードの横幅・高さのさらなる圧縮を試みている。

- グリッド：`minmax(220px, 240px)` → `minmax(200px, 220px)`に変更（適用済み）
- カード内パディング：12px（変更前から実際は12pxだった。16pxという記述は誤りだったため訂正）
- アイコンサイズ：56×56px → 48×48pxに縮小（適用済み）
- カテゴリ名の省略表示（14-1のtruncate）があるため、200px幅でも「美容・コスメ」等の長いカテゴリ名は致命的には崩れない見込み（Claude Design側のコード上の計算では約138px確保、15pxフォントなら省略なしで収まる見込みとの回答）

**計算上の見積もり（2026/08/03、実測値ではない点に注意）：**

Claude Designの視覚検証エージェントは不具合の有無のみを判定する仕組みで、生のcomputed値（実測px）は返さない。そのため、以下はコード上の数値（グリッド幅・パディング・行高さなど）から手計算で見積もった参考値であり、ブラウザでの実測ではない。

| 項目 | 見積もり値 |
|---|---|
| カード1枚の高さ | 約210〜214px |
| 列数（iMac・コンテンツ幅約2144px想定） | 約9列（従来の7〜8列から増加） |
| 1ページ分（9列×3行）のグリッド高さ | 約682px |
| ヘッダー〜3行目までの合計高さ | 約950〜970px |
| iMac 24インチ論理解像度（幅2240×高さ約1260px想定）でのスクロール | 合計約970px＜1260pxのため、縦スクロールなしで収まる見込み |

**残る不確実要素：**
- 上記はあくまで計算上の見積もりのため、実際のフォントレンダリング・line-height・ブラウザ差分などで数十px単位のズレが出る可能性がある
- 「スクロールが少し増えた気がする」という体感報告の原因は未特定のまま
- 正確なcomputed値が必要な場合は、視覚検証エージェントに実測を明示的に依頼するか、実機のDevToolsで直接確認する
- Windows機（訓練校デスクトップ）は解像度・スケーリングの仕組みがMacと異なるため、この見積もりがそのまま当てはまらない可能性がある（次項参照）

**実装確認済み（Claude Designに直接確認）：**

- ページ全体の最大幅（`max-width:2400px; margin:0 auto`）は`<main>`（商品グリッドを包む一番外側の要素）とヘッダー内の内側divに、inline styleで正しく適用されていることを確認（17番の実装完了）
- グリッドは当初`grid-template-columns:repeat(auto-fill, minmax(var(--grid-min),1fr))`のまま・`justify-content`未指定（`normal`）になっており、`auto-fit`への変更が漏れていたことが判明。Claude Designに直接指摘し、`auto-fit` + `justify-content:center`に修正済み
- Windows機（訓練校デスクトップ）での列数はまだ未確認。解像度・スケーリング設定によってMacと異なる見え方になる可能性があるため、実機確認時に追記予定

### 13-3. ヘッダー・検索行の縦パディングを絞る

```jsx
// Before
<header className="py-8">

// After
<header className="py-4 lg:py-6">
```

### 13-4. 検索バーの高さをタップ領域基準（44px）に揃える

```jsx
// Before
<input className="py-4 text-base" />

// After
<input className="py-3 text-base min-h-[44px]" />
```

- 5番（`min-h-[44px]`ルール）と統一。それ以上高くする必要はない

### 13-5. 画面サイズに依存しない拡大縮小（連続スケーリング）について

「ノートPCでもデスクトップでも、アイコンやボックスが画面サイズに応じて比率を保ったまま連続的に拡大縮小する」という発想の実装メモ。

**基準となる画面幅（最小・最大）**

| 基準 | 画面幅 | 想定端末 |
|---|---|---|
| 最小 | 1280px | 小さめのノートPC（MacBook Air 13.6インチ相当） |
| 最大 | 1920px | 一般的なデスクトップモニター（フルHD） |

この範囲内でのみ連続的にスケーリングし、範囲外（1280px未満・1920px超）ではそれ以上縮小・拡大しない。

**適用してよいもの：アイコン・ボックス・余白**

```css
.icon-box {
  /* 1280px幅で50px、1920px幅で90pxになるよう計算した式 */
  width: clamp(50px, calc(-30px + 6.25vw), 90px);
  aspect-ratio: 4 / 3; /* 縦横比は常に固定 */
}
```

検証済み：1280px時＝50px、1920px時＝90px、範囲外はclamp()が自動で上下限に固定。

**適用しないもの：文字サイズ（縮小させない）**

- 商品名21px（10番）などの文字サイズは、画面幅に関わらず常に固定
- `vw`ベースでの可変は行わない（小さい画面で文字が縮んで読みにくくなることを避けるため）

```css
.product-name { font-size: 21px; } /* 画面幅に関係なく固定 */
```

→ 結論：**アイコン・ボックス・余白は1280px〜1920pxの範囲で連続スケーリング、文字サイズは常に固定**というハイブリッド方針とする。

---

## 14. 商品カード内レイアウトの横並び化・コンパクト化

商品カード内で、カテゴリ行と在庫数行がそれぞれ単独の行になっており、右側の空白が目立ち縦の高さも無駄に消費していた。この2つを1行にまとめ、カテゴリを左・在庫数を右に配置する。また、在庫少の警告を「全文テキストの別行バッジ」から「在庫数の右に⚠️アイコン」へ変更し、さらに縦の高さを削減する。

### 14-1. カテゴリと在庫数を横並びに

```jsx
<div className="flex justify-between items-center gap-2">
  <span className="inline-flex items-center gap-1 min-w-0 flex-1">
    <span className="text-2xl flex-shrink-0">✏️</span>
    <span className="text-sm text-gray-600 font-medium truncate">文房具</span>
  </span>
  <span className="text-2xl font-bold flex items-center gap-1 flex-shrink-0">
    {isLowStock && <span title="在庫が少なくなっています" className="text-red-500">⚠️</span>}
    <span className={isLowStock ? "text-red-500" : ""}>2</span>
    <span className="text-sm font-normal">冊</span>
  </span>
</div>
```

- **カテゴリ名にも`truncate`を追加**（当初は商品名10番のみに設定しており、カテゴリ名は未対応だった。見落としを修正）
- `min-w-0`：flexコンテナ内で`truncate`を正しく効かせるために必要
- `flex-1`（カテゴリ側）：余った幅を使いつつ、足りなければ省略表示
- `flex-shrink-0`（在庫数側）：在庫数は縮まない・折り返されない。常にフル表示を保証
- **カテゴリ絵文字サイズ：`text-2xl`（24px）** — 実機確認の結果、7番の基準（14pxの1.3〜1.5倍＝18〜21px）では「まだ小さい」と感じたため、在庫数の文字（`text-2xl`）と同じ24pxまで引き上げ。数値の目安より実際の見た目を優先した調整
- 文字サイズ（カテゴリ・在庫数）自体は変更なし

### 14-2. 在庫少の警告を「別行のテキストバッジ」→「在庫数横の⚠️アイコン」に変更

**経緯：** 従来の「⚠ 在庫が少なくなっています」という全文テキストバッジ（別行・カード全幅）を廃止し、在庫数の右側に⚠️アイコンを添えるだけの表示に変更する。

```jsx
<span className="text-2xl font-bold flex items-center gap-1">
  {isLowStock && (
    <span title="在庫が少なくなっています" className="text-3xl text-red-500 leading-none">⚠️</span>
  )}
  <span className={isLowStock ? "text-red-500" : ""}>2</span>
  <span className="text-sm font-normal">冊</span>
</span>
```

- **アイコンサイズ：`text-3xl`（30px）** — 在庫数の文字（`text-2xl`＝24px）より一回り大きくする（初期反映時はサイズ指定が抜けて小さすぎたため追記）
- `leading-none`：絵文字の行間による位置ズレ防止
- **🔺ではなく⚠️（warning sign）を使う**：🔺は株価などで「上昇」を意味することがあり、「在庫が少ない＝注意」の意味と誤解されるリスクがあるため
- `title`属性でホバー時に全文（「在庫が少なくなっています」）を表示 → プレゼン原稿Q&Aの「色だけに頼らない、アイコンやテキストラベルを併用する」方針は、アイコン＋ホバーテキスト＋数字の赤色化（3つの非色情報）で引き続き満たす
- 在庫少の時は在庫数自体も赤色にする（アイコン・色の2重サイン）

**副次的な発見：** 警告バッジが別行として存在する・しないで、在庫少の商品だけカードが高くなり、グリッドの高さが揃わない問題があった。この変更により、在庫状況に関わらず全カードが同じ高さになる。

**実装時の注意（判定ロジック）：** 実機確認で、在庫0個の商品に⚠️が付かない不具合が見つかった。`isLowStock`の判定を`stock < threshold`だけにすると、発注点（threshold）が未設定・0の商品は、在庫0でも警告が出ない。以下のロジックにすること。

```js
// Before（在庫0・発注点0の場合に警告が出ない可能性）
const isLowStock = stock < threshold;

// After（在庫0は発注点に関わらず必ず警告、それ以外は発注点以下で警告）
const isLowStock = stock === 0 || stock <= threshold;
```

**アプリ全体の色ルール（2026/08/03明文化）：** 「オレンジ＝残りわずか／赤＝在庫切れ／緑または白グレー＝正常」に統一。トップページのカード内在庫数字・ステータスバッジ、商品詳細画面の「現在の在庫」ボックス（緑/オレンジ/赤の3段階）ともに実装・確認済み。

### 14-3. サイズの再計算（警告テキスト削除に伴う縮小）

警告の全文テキストがなくなったことで、カードの横幅を制限する要素が「カテゴリ＋在庫数の横並び行」に変わる。

- カテゴリ最大想定（例：「美容・コスメ」6文字＋アイコン）≈ 108px
- 在庫数最大想定（4桁＋単位＋警告アイコン）≈ 94px
- カード左右パディング ≈ 32px
- 合計 ≈ 234px

→ **カード最小幅：260px → 240px に変更**（13-2のminmax値もこれに合わせて更新）
→ **カード高さ目安：334px → 250px前後**（警告別行の廃止・カテゴリ&在庫数の統合による削減。実装後に実測で微調整）

- 在庫少の警告バッジ（別行）はこの変更の対象外。低在庫時のみ引き続き表示

**注意（実装確認時）：** カード幅を240pxより狭く（例：215px）した検証で、隙間だけが不自然に広がる現象が見られた。これは「カード幅は縮めたが、グリッドが`auto-fill`ではなく固定列数のままだった」ために起きた可能性が高い。実装時は、必ず13-2の`grid-template-columns: repeat(auto-fill, minmax(240px, 1fr))`が正しく適用されているか確認すること。また、240px未満はカテゴリ＋在庫数の行が折り返されるリスクがあるため、下限として維持する。

**Claude Design側の実装詳細（density preset）：** Claude Designでは`--grid-min`の値を`densityMap`という密度プリセット（「ゆったり」200px／「コンパクト」170px）から取得する仕組みになっていた。当初「どちらのプリセットも折り返しリスクがある」と判断していたが、14-1でカテゴリ名にも`truncate`を追加したことで、幅が狭くても「折り返し」ではなく「…で省略」される設計になった。そのため200px前後のプリセットでも致命的な崩れは起きないが、あまりに狭いと「✏️ 文…」のようにカテゴリがほぼ読めなくなるため、200px前後は読みやすさの観点で妥当なライン。240pxへの変更は必須ではなくなったが、より余裕を持たせたい場合の選択肢として残す。

---

## 15. 空の状態（Empty State）

トップページが「商品がある前提」でしか設計されておらず、商品0件・検索結果0件のときに空白画面になる問題があった。パソコン操作が苦手な人ほど「何も起きていない画面」で不安になりやすいため、2パターンの空状態を用意する。

### 15-1. 商品が1件も登録されていない時

- 見出し：「商品がまだ登録されていません」
- 補足：「「＋新しい商品を登録」から商品を追加しましょう」
- アイコン（📦など）＋、既存の「新しい商品を登録」ボタンを中央に大きめに配置

### 15-2. 検索・フィルターで該当0件の時

- 見出し：「該当する商品が見つかりませんでした」
- 補足：「検索条件やフィルターを変更してみてください」
- 商品はすでに存在する状態なので、登録ボタンは出さない（出すと紛らわしいため）

```jsx
{products.length === 0 ? (
  <EmptyState
    icon="📦"
    title="商品がまだ登録されていません"
    message="「＋新しい商品を登録」から商品を追加しましょう"
    showAddButton
  />
) : filteredProducts.length === 0 ? (
  <EmptyState
    icon="🔍"
    title="該当する商品が見つかりませんでした"
    message="検索条件やフィルターを変更してみてください"
  />
) : (
  <ProductGrid items={filteredProducts} />
)}
```

## 16. 検索ボックスの最大幅を制限

現状、検索ボックスが横幅いっぱいに広がっており、「商品名で検索」という短いプレースホルダーに対して不釣り合いに長い。最大幅を決めてコンパクトにする。

```jsx
// Before
<input className="w-full ..." placeholder="商品名で検索" />

// After
<input className="w-full max-w-sm ..." placeholder="商品名で検索" />
```

- `max-w-sm`（384px）を目安に、実装後に見た目を見ながら微調整可

---

## 17. ページ全体のコンテンツ最大幅

検索欄・カテゴリ・フィルタータブ・「＋新しい商品を登録」ボタンを1行にまとめたところ、画面が広い（大画面デスクトップなど）と、フィルタータブと登録ボタンの間に大きな空白ができ、ボタンだけ画面端に孤立して見える問題が発生した。

**原因：** ページのコンテンツが画面幅いっぱいに伸びる作りになっており、画面が広くなるほど要素間の余白も比例して広がってしまうため。

**対応：** ページ全体（ヘッダー・検索行・商品グリッドを含む）を、最大幅を持つコンテナで包む。

```jsx
<div className="max-w-[2400px] mx-auto px-6">
  {/* ヘッダー・検索行・商品グリッドなど全部この中 */}
</div>
```

- 画面がどれだけ広くても、コンテンツは2,400px前後で頭打ちになり、左右に均等な余白ができる
- 検索・カテゴリ・フィルター・登録ボタンの配置バランスが、画面サイズに関係なく安定する

**数値の更新（2026/08/03）：** 当初1,800pxで運用していたが、訓練校iMac（24インチ）実機確認で「商品カードの列数をもっと増やしたい」というフィードバックを受け、13-2のカード幅上限見直し（280px→240px）と合わせて2,400pxに拡張した。13-5の連続スケーリング基準（1,280px〜1,920px）とは独立した「コンテンツの絶対上限」という位置づけのため、この数値自体は13-5と矛盾しない。詳細な経緯・実機確認結果は13-2を参照。

---

## 18. アクセシビリティ全面改修（PC操作が苦手な人・高齢者・店舗スタッフ向け）

企画発表を見据え、「初めてパソコンを使う人でも説明書なしで使える」を目標にトップページを全面的に見直した。以下の項目は一部、2番・13-1・14番の内容を上書き・更新するもの。

### 18-1. ヘッダー

- 店舗名はタイトルより控えめに。「店舗：〇〇商店」という表記に変更
- タイトル「やさしい在庫管理」は変更なし（11番のまま）

### 18-2. 検索・フィルター

- 検索欄の上に「商品を探す」ラベルを追加
- 「全カテゴリ」→「商品の種類」に文言変更
- フィルター（すべて／在庫少／在庫あり）の上に「表示する商品」の見出しを追加
- 選択中のボタンを塗りつぶし（青）にして、切り替えであることを明示

### 18-3. 「次にやること」パネル（新規）

ホーム画面上部に状態サマリーを表示する。

- 「⚠ 在庫が少ない商品が◯件あります」「✕ 在庫切れの商品が◯件あります」「📦 今日の入出庫は◯件です」
- **当初は縦並びで実装され、右側に大きな空白ができる問題があったため、横並びのバッジ形式に修正**（在庫切れ／在庫少／入出荷を横一列に並べる）

### 18-4. 商品カードの画像プレースホルダーを縮小（2番・13-1を更新）

これまでの「横幅いっぱいのバナー型プレースホルダー（`--img-h`：ゆったり120px／コンパクト88px）」から、**56×56px程度の小さい正方形アイコン**に変更。カテゴリ名をアイコンのすぐ右に横並び表示する構成にした。

- 理由：在庫管理アプリは商品を「販売」するページではなく、商品名とカテゴリが分かれば十分と判断。写真の優先度を下げ、カード全体のコンパクト化を優先
- 商品画像アップロード自体は任意項目として維持（必須化はしない、優先度低）
- カテゴリ表示位置の指示漏れ（アイコンの右に来るはずが別行になっていた）があり、修正依頼して反映済み

### 18-5. ステータス表示をバッジに統合（14-2を更新）

14-2で「在庫数横の⚠️アイコン」としていたが、実装したところ「⚠補充」ラベルと「残りわずか」ステータスバッジが両方在庫数の行に並び、文字がカードからはみ出す問題が発生。以下の形に統合した。

- ステータスバッジ自体にアイコンを含める：「在庫あり」／「⚠ 残りわずか」／「⚠ 在庫切れ」
- 独立した「補充してください」の行は廃止し、`title`属性のホバーツールチップに全文を退避
- 色だけに頼らずラベル文字でも状態が分かる方針（プレゼン原稿のQ&Aと整合）は維持
- 判定ロジック（`stock === 0 || stock <= threshold`）は変更なし（14-2のまま）

### 18-6. 発注目安はカードから削除、詳細画面のみに表示

一覧のカードは「今すぐ判断が必要な情報（在庫数・状態）」に絞り、発注目安（発注点）は詳細画面に表示する設計に変更。カードの高さをさらに圧縮する目的。

### 18-7. カードへの「＋入庫／－出庫」設置は見送り

企画発表向けの案として、カードに直接「＋入庫」「－出庫」ボタンを置く案を一度実装したが、カード1枚の高さが大きくなりすぎ、商品数が多い場合にスクロールが増える問題が発覚。**最終的にはカードから削除し、入出庫操作は詳細画面のみで行う設計に戻した。** カードは「アイコン＋商品名＋カテゴリ＋在庫数＋ステータスバッジ＋詳細を見るボタン」のみのシンプル構成。

### 18-8. カード同士が重なる不具合の修正

商品を新規登録すると、カード同士が重なって表示される不具合があった。原因はカード幅が固定値（`var(--card-w)`）になっており、実際のグリッドセル幅と一致していなかったため。`width: 100%`にしてグリッドセルに追従させることで解消。

### 18-9. 操作後のフィードバック

- 登録／編集／削除／入庫／出庫が成功した際は、画面右上にトースト通知を表示（例：「✅ 保存しました」「✅ 在庫を更新しました」）
- 削除ボタンを押した際は、必ず確認ダイアログ（「本当に削除しますか？」＋キャンセル／削除するボタン）を経由してから実行する

### 18-10. アイコン画像の生成AI利用は不採用

ChatGPT（チャッピー）で「補充してください」を訴求する赤いアイコン画像を作成し、採用を検討したが不採用と判断。

- 2番で「絵文字ベース・アイコンライブラリ非導入」の方針をすでに決めており、外部画像アセットを導入すると管理・読み込み・色変更のしやすさの面で矛盾する
- 特に文字（「補充してください」）が画像に焼き込まれたバージョンは、文字サイズ変更・多言語対応ができず、スクリーンリーダー等のアクセシビリティ観点でも不利なため、UI文字は常にHTML側のテキストとして持つべきと判断

### 18-11. 「次にやること」パネルを非クリック要素として明示（18-3を更新）

チャッピーによるトップページ最終チェックで、「次にやること」パネルが背景色付き・角丸のバッジ形式になっていることで、ボタンやタブのように「押せそう」に見えてしまう指摘があった。実際は状態を表示するだけの情報パネルなので、見た目で「情報」であることを明示する。

- 見出しを「次にやること」→「今日のお知らせ」に変更（行動を促す文言→状態を伝える文言へ）
- パネル全体の背景をカード等のクリック要素より薄い色にし、ホバー時の色変化・枠線変化（12-3のクリック可能性明示ルール）を**付けない**ことで、ボタンと視覚的に区別する
- 中身（在庫切れ／残りわずか／入出荷件数の横並びバッジ形式）自体は18-3のまま変更なし

```jsx
// Before（ボタンっぽく見える）
<div className="flex gap-2">
  <span className="px-3 py-1 rounded-full bg-red-50 text-red-600">在庫切れ 2件</span>
  ...
</div>

// After（情報パネルであることを明示）
<section aria-label="今日のお知らせ">
  <h2 className="text-sm text-gray-500 font-medium mb-2">今日のお知らせ</h2>
  <div className="flex gap-2">
    <span className="px-3 py-1 rounded-md bg-gray-50 text-red-600">⚠ 在庫切れ　2件</span>
    ...
  </div>
</section>
```

### 18-12. 「詳細を見る」ボタンの視認性強化

テキストのみの「詳細を見る」は初心者にとって「何の詳細か」が伝わりにくいという指摘があった。方向を示すアイコンを添えて、遷移することを直感的に伝える。

```jsx
// Before
<button>詳細を見る</button>

// After
<button className="flex items-center justify-center gap-1">
  <span aria-hidden="true">▶</span>
  詳細を見る
</button>
```

- 12-3（クリック可能性の明示）の一環として、矢印アイコンで「進む」感を補強する

### 18-13. 在庫表記を1行にまとめる（14-1を更新）

「在庫」ラベルと数値が縦に離れて表示されていたが、分ける必要性は薄く、1行にまとめてカードの高さをさらに圧縮する。

```jsx
// Before
<div>
  <span className="text-sm text-gray-500">在庫</span>
  <span className="text-2xl font-bold">24</span>
  <span className="text-sm">本</span>
</div>

// After
<div className="text-2xl font-bold">
  在庫<span className="text-2xl">24</span>本
</div>
```

- 文字サイズ自体（14-1・10番）は変更なし、レイアウトのみ1行化

### 18-14. ステータスバッジの色バランス調整（18-5を更新）

「在庫あり」バッジが緑色で表示されており、正常な商品も含め全体的に色が目立ちすぎて、本来注目してほしい異常（在庫切れ・残りわずか）が埋もれてしまうという指摘があった。「人は異常だけ見たい」という原則に基づき、正常時の色を抑える。

- 「在庫あり」：緑→グレー系（`bg-gray-50 text-gray-600` 等）に変更し、目立たせない
- 「⚠ 残りわずか」「⚠ 在庫切れ」：引き続き赤・オレンジ系で目立たせる（変更なし）
- 12-2（意味を持つ色は最大5〜6色まで）の趣旨に沿い、「正常＝目立たない」「異常＝目立つ」のメリハリを強める

### 18-15. 店舗名のリンク視認性

「田中商店」をクリックして店舗情報画面（8番）へ遷移する設計だが、見た目がリンクだと分かりにくいという指摘があった。

```jsx
// Before
<span>店舗：田中商店</span>

// After
<button className="inline-flex items-center gap-1 text-gray-700 hover:text-blue-600">
  🏪 店舗：田中商店 <span aria-hidden="true">›</span>
</button>
```

- 12-3（クリック可能性の明示）に沿い、アイコン（🏪）と「›」で遷移可能であることを示す

### 18-16. 在庫数表記の視認性強化（18-13を更新）

チャッピーによるレビュー第2弾で、「在庫24本」の表記が読みづらい、数字を主役にした方が視認性が上がるという指摘があった。ただし2行に分ける案は、18-13（1行化によるカード高さ圧縮）・13-2/20番（3行表示でスクロールなしに収める計算）と矛盾するため不採用。**1行のまま、ラベルを縮小・数字を強調する形で対応する。**

```jsx
// Before（18-13）
<div className="text-2xl font-bold">
  在庫<span className="text-2xl">24</span>本
</div>

// After（数字を主役に、1行のまま）
<div className="flex items-baseline gap-1">
  <span className="text-xs text-gray-400">在庫</span>
  <span className="text-2xl font-bold">24</span>
  <span className="text-sm font-normal">本</span>
</div>
```

- 「在庫」ラベルを`text-xs text-gray-400`に縮小し、数字（`text-2xl font-bold`）とのコントラストで主役を明確化
- カードの高さには影響しない（1行のまま）ため、13-2・20番の見積もりと矛盾しない

### 18-17. 「詳細を見る」ボタンの文言短縮（12-4・18-12を更新）

チャッピーによるレビュー第2弾で、全カード共通で「詳細を見る」は少し存在感が強い、「見る ▶」でも意味は伝わるという指摘があり、コンパクトさ優先で採用。

```jsx
// Before（18-12）
<button className="flex items-center justify-center gap-1">
  <span aria-hidden="true">▶</span>
  詳細を見る
</button>

// After
<button className="flex items-center justify-center gap-1">
  <span aria-hidden="true">▶</span>
  見る
</button>
```

- 12-4（ボタン文言は具体的な動詞で統一）とは完全には一致しない（「見る」は動詞だが対象語が省略されている）が、カード内での文脈（商品名・カテゴリ・在庫がすでに表示された状態での「詳細へ進む」ボタン）と矢印アイコンの併用により、意味は十分伝わると判断
- カード内の余白確保・コンパクト化を優先する判断として採用（意図的な12-4からの逸脱として記録）

### 18-18. 店舗名表記の簡略化（18-15を更新）

チャッピーによるレビュー第2弾で、「店舗：」というラベルが冗長という指摘があった。18-15ですでに🏪アイコン＋「›」シェブロンでクリック可能性を明示しているため、「店舗：」の文字は削除する。

```jsx
// Before（18-15）
<button className="inline-flex items-center gap-1 text-gray-700 hover:text-blue-600">
  🏪 店舗：田中商店 <span aria-hidden="true">›</span>
</button>

// After
<button className="inline-flex items-center gap-1 text-gray-700 hover:text-blue-600">
  🏪 田中商店 <span aria-hidden="true">›</span>
</button>
```

- 🏪アイコンが「店舗」の意味をすでに担っているため、テキストラベルは店舗名のみで十分

### 18-19. 「今日のお知らせ」パネル見出しの変更（18-11を更新）

チャッピーによるレビュー第2弾で、パネルの中身が通知というよりダッシュボード的な状態サマリーであり、「今日のお知らせ」より実態に合った見出しの方が良いという指摘があった。18-11で一度「次にやること」→「今日のお知らせ」に変更した経緯があるが、内容（在庫切れ／残りわずか／入出荷件数の横並びバッジ）とより整合する文言として、**「現在の状況」に変更**する。

```jsx
// Before（18-11）
<h2 className="text-sm text-gray-500 font-medium mb-2">今日のお知らせ</h2>

// After
<h2 className="text-sm text-gray-500 font-medium mb-2">現在の状況</h2>
```

- 18-11で確立した「非クリック要素として明示する」方針（背景を薄く・ホバー変化なし）は変更なし。見出し文言のみの変更
- 「お知らせ」＝通知、というより「今この瞬間の状態」を表すパネルであることを、より正確に伝える文言

### 18-20. 見出し文字の視認性強化（18-2・18-11/18-19を更新）

チャッピーによるレビュー第3弾で、「現在の状況」「商品を探す」「表示する商品」の見出しがやや存在感が弱い、あと一段階だけ太く・濃くすると読みやすいという指摘があり、採用。

```jsx
// 「現在の状況」（18-19より）
// Before
<h2 className="text-sm text-gray-500 font-medium mb-2">現在の状況</h2>
// After
<h2 className="text-sm text-gray-600 font-semibold mb-2">現在の状況</h2>

// 「商品を探す」「表示する商品」ラベル（18-2より）
// Before
<label className="text-[13px] text-gray-500 font-semibold">商品を探す</label>
// After
<label className="text-[13px] text-gray-500 font-bold">商品を探す</label>
```

- 色・サイズは変えず、太さだけ一段階上げる（`font-medium`→`font-semibold`、`font-semibold`→`font-bold`）
- 18-11で確立した「非クリック要素」の見た目（背景薄め・ホバー変化なし）とは矛盾しない。あくまで見出しとしての可読性向上

**あわせて確認：カテゴリ絵文字サイズの実装ズレ**

チャッピーが「絵文字をもう少し大きく」と指摘した点は、実は新しいルールではなく14-1ですでに「カテゴリ絵文字は`text-2xl`（24px）」と決まっていたもの。Claude Design側の実装が22px（14-1の指定値より少し小さい）になっていたための指摘だったので、次回のClaude Design確認時に24pxへ修正依頼すること（design-tokens自体の変更は不要）。

### 18-21. カード全体をクリック可能にする（18-17・18-12・18-7を更新）

チャッピーによるレビュー第3弾で、カードをクリックして詳細へ行けるなら「見る」ボタンをなくし、カード全体をクリック可能にした方が良いという提案があった。メインペルソナ（PC操作が苦手な人・高齢者）にとって、クリック領域が大きいほど操作ミスが減るという判断から採用。

- **カード全体が詳細画面へのクリック領域になる**（ボタンだけでなく、アイコン・商品名・カテゴリ・在庫数の上でもクリック可能）
- 「見る ▶」表示は機能的なボタンではなく、カード下部の「進む」ことを示す装飾的な表示として残す（見た目は18-17のまま変更なし）
- 12-3（クリック可能性の明示）は、すでに実装済みのカード全体のホバー効果（影・枠線の変化）で引き続き担保される。新たにゼロから作る必要はない

```jsx
// Before（18-17：ボタンのみがクリック領域）
<div className="card">
  {/* アイコン・商品名・カテゴリ・在庫数 */}
  <button className="flex items-center justify-center gap-1" onClick={goToDetail}>
    <span aria-hidden="true">▶</span>
    見る
  </button>
</div>

// After（カード全体がクリック領域、「見る▶」は装飾）
<a href={`/items/${id}`} className="card block hover:shadow-md hover:border-gray-300 transition">
  {/* アイコン・商品名・カテゴリ・在庫数 */}
  <div className="flex items-center justify-center gap-1 border rounded-lg min-h-[44px] mt-auto" aria-hidden="true">
    <span>▶</span>
    見る
  </div>
</a>
```

**実装時の注意点：**
- カード要素は`<button>`のネストを避けるため`<a>`タグ（またはdiv+onClick+role="button"+tabIndex={0}）で実装し、キーボード操作（Tab移動・Enterで遷移）にも対応させること
- 「見る ▶」部分は装飾のため`aria-hidden="true"`にし、スクリーンリーダーがカード全体のリンクと二重に読み上げないようにする（カード自体のアクセシブルネームは商品名で十分）
- 18-7（カードへの入出庫ボタン設置は見送り）の判断はそのまま維持。カード全体クリックはあくまで「詳細画面へ」の遷移のみで、入出庫操作をカードに追加するものではない
- 18-12は本項目により実質的に上書き（ボタンとしての「見る」は廃止、装飾表示として存続）

### 18-22. キーボードフォーカスの可視化

キーボード操作（Tabキーでの移動）でテスト確認したところ、「商品の種類」セレクトなど一部要素でフォーカスが当たっているかどうか見た目で分からない箇所があった。18-21（カード全体クリック化）でキーボード操作（Tab＋Enter）対応を追加したこともあり、フォーカス表示のルールを全要素共通で統一する。

- `:focus-visible` を使用し、**キーボード操作時のみ**青枠を表示する（マウスクリック時には表示しない）
- マウスクリックでもフォーカスリングが出る`:focus`ではなく`:focus-visible`を使う理由：クリック操作の人にとっては不要な枠線に見えてしまい、12-2（余計な装飾色を使わない）の趣旨とも合わないため

```css
/* 全ボタン・入力欄・リンク・カード（18-21のaタグ）共通 */
:focus-visible {
  outline: 2px solid #2563eb; /* リンク色（12-2の青）と統一 */
  outline-offset: 2px;
}
```

- 対象：ボタン全般（保存する／削除する等）、入力欄、セレクト（商品の種類など）、カード全体リンク（18-21）、フィルタータブ、ページネーションボタン（20番）
- 12-3（クリック可能性の明示）とセットの原則：マウスでもキーボードでも「今どこを操作しているか」が分かるようにする

### 18-23. 在庫切れカードの背景色強調（14-2・18-5を更新）

Claude Design側の実装で、在庫切れ（`stock === 0`）の商品カードに、ステータスバッジだけでなくカード全体（背景・枠線）にも赤みがかった色を付ける表現が追加されているのを確認した。

**経緯：** 当初（14-2・18-5）は「カード自体の色は変えず、ステータスバッジの色・アイコン・ホバーテキストのみで異常を示す」方針だったが、実際の画面で確認したところ「在庫切れがひと目で分かりやすい」という判断から、この表現を正式に採用することにした。

- **在庫切れ（`stock === 0`）のカードのみ**、背景をごく薄い赤（例：`bg-red-50`）、枠線を赤系（例：`border-red-200`）にする
- **残りわずか（在庫あり・発注点以下）のカードは対象外**：カード自体の色は変えず、従来通りステータスバッジ（オレンジ系）のみで示す。在庫切れとの強弱の差をつける
- ステータスバッジ・アイコン・ホバーテキストによる非色情報の表示（18-5）は変更なし。カードの背景色はあくまで追加の強調であり、色だけに頼る表現ではない（12-2「色以外の情報でも伝わるようにする」方針は維持）

```jsx
// 在庫切れカードのみ背景・枠線を変更
<div className={`card ${stock === 0 ? "bg-red-50 border-red-200" : "bg-white border-gray-200"}`}>
  ...
</div>
```

- 12-2（意味を持つ色は最大5〜6色まで）との整合：新たな色を追加するのではなく、既存の「赤＝警告・発注点未満」の色をカード背景にも展開する形のため、色数は増えていない
- 対象は在庫切れのみに絞ることで、「在庫切れ」と「残りわずか」の緊急度の違いが視覚的にも伝わるようにする

---

## 19. ローディング・処理中表示

チャッピーによるトップページ最終チェックで提案された項目。ボタンを押した後、処理中であることが分からないと、PC操作が苦手な人ほど「反応がない」と感じて何度もクリックしてしまい、二重登録・二重削除につながるリスクがある。

- ボタンを押してから処理が完了するまでの間、ボタンのラベルを「処理中であることが分かる文言」に一時的に差し替える
- 処理中はボタンを`disabled`にし、連打による二重送信を防ぐ

```jsx
// 例：保存ボタン
<button disabled={isSaving} className="... min-h-[44px] disabled:opacity-60">
  {isSaving ? "保存中..." : "保存する"}
</button>
```

| 操作 | 通常表示 | 処理中表示 |
|---|---|---|
| 保存する | 保存する | 保存中... |
| 登録する | 登録する | 登録しています... |
| 削除する | 削除する | 削除しています... |
| 入出荷を記録 | 入出荷を記録 | 記録しています... |

- 完了後は18-9の方針通り、画面右上にトースト通知（「✅ 保存しました」等）を表示し、ボタンは通常表示に戻す
- 理由：①クリック連打の防止　②「動いている」と分かる安心感　③二重登録・二重削除の防止
- **商品詳細画面（22番）で実装・動作確認済み**：削除確認ダイアログの「削除する」ボタンで「削除しています...」＋両ボタンdisabledを確認
- **登録フォーム（24〜25番参照）でも実装・動作確認済み**：「保存する」→「保存しています...」の表示を確認

---

## 20. 商品一覧のページネーション

商品が100件規模になった場合の表示方式について、無限スクロールか、ページ区切り＋ページ送りボタンかを検討した。

**採用：ページネーション（1ページの件数は「列数×3行」で動的算出）**

理由：

- **予測可能性：** 「今何ページ目にいるか」が明確になり、パソコン操作が苦手な人ほど自分の位置が分かりやすい（12番のUI原則と一致）
- **操作の分かりやすさ：** 無限スクロールは下にスクロールし続けるほど「最初に戻る」操作が分かりにくくなりがちで、初心者向けの本アプリの方針と合わない
- **実装のシンプルさ：** LocalStorage＋React管理という構成上、`slice`ベースで完結し、無限スクロールのような「どこまで読み込んだか」の状態管理を増やさずに済む

**1ページの件数：固定値ではなく、実際の列数から動的算出**

当初は固定20件で設計していたが、13-2・17番でグリッドの列数を実機に応じて調整した経緯を踏まえ、**画面幅によって実際に並ぶ列数が変わる（7列・8列など）以上、1ページの件数も固定するとバランスが崩れる**という判断から、動的算出方式に変更した。

- `ResizeObserver`でグリッドの実際の列数を測定
- 「列数×3行」を1ページの表示件数とする（例：7列→21個、8列→24個）
- 画面幅が変わる（ウィンドウリサイズ・別モニターでの表示など）と自動で再計算される
- 3行分を1ページの目安にしているのは、スクロールなしで一目に把握できる密度を優先したため（13番のFV最適化方針と一致）

```jsx
const [columns, setColumns] = useState(4); // 初期値
const PAGE_SIZE = columns * 3;

// ResizeObserverでグリッドのcolumn数を測定し、setColumnsで更新
const totalPages = Math.ceil(filteredProducts.length / PAGE_SIZE);
const pageItems = filteredProducts.slice(
  (currentPage - 1) * PAGE_SIZE,
  currentPage * PAGE_SIZE
);
```

- 画面幅が変わってページ件数が変動した際、現在表示中のページ番号が総ページ数を超えてしまう場合は、1ページ目に戻すなどの調整が必要（実装時に要確認）

**表示条件：** 総商品数（フィルター後）が1ページの表示件数（動的算出値）以下の場合は、ページネーション自体を非表示にする。

**ページネーションのUI詳細・配置・固定表示については27番を参照（20番の内容を大幅に更新）。**

---

## 21. 「商品の種類」と「表示する商品」を「絞り込み」として統合

チャッピーからのレビューで、「商品を探す（検索）」に対して「表示する商品（ステータスフィルター）」の見出しがやや離れて見え、検索・カテゴリ・表示切替がバラバラな操作に見えるという指摘があった。

**検討した案：**

1. チャッピー案：検索・カテゴリ・表示切替を1つの枠でまとめて囲む
2. 採用案：検索はそのまま独立させ、「商品の種類（カテゴリ）」と「表示する商品（ステータス）」の2つだけを1つの見出し「絞り込み」の下にまとめる

**採用案を選んだ理由：**

- 「商品を探す（検索）」は**自由入力**、「商品の種類」「表示する商品」は**選択式の絞り込み**で、操作の性質が異なる。異なる性質の操作は分けたままの方が、今何をしているか伝わりやすい
- 枠線で囲むと、すでにある「現在の状況」パネル（18-3）と合わせて箱だらけになり、12-2（意味を持つ色は最大5〜6色まで／装飾のためだけの色は使わない）の方針と衝突する
- 見出しの統合＋余白の調整だけで、枠線を使わずにグループ感を出せる

**実装イメージ：**

```jsx
// Before（18-2）
<div className="field">
  <label>商品の種類</label>
  <select>...</select>
</div>
<div className="field">
  <label>表示する商品</label>
  <div className="filter-tabs">すべて／在庫少／在庫あり</div>
</div>

// After
<div className="field">
  <label className="text-[13px] text-gray-500 font-bold">絞り込み</label>
  <div className="flex items-center gap-4">
    <select>...（商品の種類）</select>
    <div className="filter-tabs">すべて／在庫少／在庫あり</div>
  </div>
</div>
```

- 見出し文言：「絞り込み」に確定（18-20のスタイル＝`text-[13px] text-gray-500 font-bold`をそのまま踏襲）
- 「商品を探す」と「絞り込み」の間の余白は、既存の要素間余白よりやや広めに取り、検索（自由入力）と絞り込み（選択式）の性質の違いを視覚的に示す
- 「商品の種類」セレクトと「すべて／在庫少／在庫あり」タブは横並びで配置し、1つの見出し「絞り込み」の下に収める
- 枠線・背景色は追加しない（12-2の方針を維持）
- フォーカス表示（18-22の`:focus-visible`）は、セレクト・タブそれぞれに引き続き適用

---

## 22. 商品詳細画面のボタン設計

ボタンを「画面遷移のみ」（編集する／入出荷を記録／削除する＝ダイアログを開くだけ）と「実データ書き込みあり」（削除確認ダイアログ内の削除する）の2種類に分類し、前者はhover明示のみ、後者はhover明示＋処理中表示（「削除しています...」＋両ボタンdisabled）を適用する方針。

- 「画面遷移のみ」ボタン：hover時の色/枠線変化（12-3）のみ。実際のデータ書き込みを伴わないため、処理中表示（19番）は不要
- 「実データ書き込みあり」ボタン（削除確認ダイアログ内の「削除する」）：hover明示＋処理中表示（19番のルール適用）＋両ボタン（削除する／キャンセル）disabled化
- この「遷移のみ vs 実処理あり」の分類基準は、登録フォーム・入出荷記録・店舗情報画面など他画面にも同様に展開（24〜26番で実装済み）

**実装・動作確認済み（2026/08/03時点）。**

---

## 23. 商品詳細画面の追加要素

### 23-1. 前後ナビゲーション「← 戻る」「次へ →」

- 商品詳細画面に前後の商品へ移動するナビゲーションを追加
- 並び順は一覧画面の絞り込み後の表示順と連動
- 最初/最後の商品ではボタンを非表示
- hover・focus-visible実装・確認済み
- 実装メモ：一覧→詳細への遷移時に「フィルター後の商品ID配列」を渡す設計がClaude Codeでの実装時に必要
- **27番で更新：** 商品点数が多い場合、実際には番号付きページネーション（1商品＝1ページ相当）と同じ構成で実装されていることを確認。20番のトップページ用ページネーションと同一の設計（27番）に統合

### 23-2. 「現在の在庫」ボックスの3段階色分け

- 商品詳細ページの「現在の在庫」ボックスを2段階（緑／赤）→**3段階（緑＝正常／オレンジ＝残りわずか／赤＝在庫切れ）**に統一
- 実装・確認済み

---

## 24. バリデーションエラー表示のルール（登録フォーム）

対象項目：商品名（必須）、在庫数・発注点（0以上の数値）

### 24-1. エラーメッセージの表示位置（インライン＋上部サマリー）

- **インライン**：エラーになった入力欄のすぐ下に、赤文字でエラー内容を表示
- **上部サマリー**：「保存する」を押した時点でエラーが残っている場合のみ、フォーム上部に「入力内容を確認してください」という見出し＋エラー項目のリンク（クリックで該当欄へスクロール＋フォーカス）を表示

```jsx
{/* フォーム上部サマリー（保存時にエラーが残っている場合のみ表示） */}
{hasErrors && (
  <div className="mb-4 p-4 bg-red-50 border border-red-200 rounded-lg">
    <p className="text-red-700 font-semibold text-sm mb-1">入力内容を確認してください</p>
    <ul className="text-sm text-red-600 list-disc list-inside">
      {errors.map(err => (
        <li key={err.field}>
          <button type="button" onClick={() => focusField(err.field)} className="underline">
            {err.message}
          </button>
        </li>
      ))}
    </ul>
  </div>
)}

{/* 各入力欄のインラインエラー */}
<div>
  <label>商品名</label>
  <input className={errors.name ? "border-red-500 bg-red-50" : "border-gray-300"} />
  {errors.name && <p className="text-sm text-red-600 mt-1">商品名を入力してください</p>}
</div>
```

### 24-2. エラー時の入力欄の見た目（枠線＋背景をうっすら赤く）

```jsx
// 通常時
className="border border-gray-300 rounded-lg px-3 py-2"

// エラー時
className="border border-red-500 bg-red-50 rounded-lg px-3 py-2"
```

- 12-2（意味を持つ色は最大5〜6色まで）の「赤＝警告」の色をそのまま流用。新しい色は増やさない
- エラーメッセージのテキスト自体が非色情報として機能するため、別途アイコンは付けない

### 24-3. エラーチェックのタイミング（フォーカスアウト＋保存時の最終チェック）

- **フォーカスアウト時**：各入力欄からフォーカスが外れた瞬間にその欄だけチェックし、インラインエラーを即座に出す／消す（早期発見）
- **保存時**：「保存する」押下時に全項目を再チェックし、1つでもエラーが残っていれば保存処理を中断し、上部サマリーを表示して最初のエラー欄にフォーカスを移す

```jsx
const handleBlur = (field) => {
  setErrors(prev => ({ ...prev, [field]: validateField(field, values[field]) }));
};

const handleSave = () => {
  const allErrors = validateAll(values);
  if (Object.keys(allErrors).length > 0) {
    setErrors(allErrors);
    focusField(Object.keys(allErrors)[0]); // 最初のエラー欄へフォーカス
    return; // 保存処理は実行しない
  }
  setIsSaving(true);
  // 保存処理（19番の処理中表示と連動）
};
```

- 19番（処理中表示）との関係：保存時チェックでエラーがなければ`isSaving`をtrueにして通常の処理中フロー（「保存しています...」）へ進む。エラーがある場合は`isSaving`にはしない

### エラーメッセージ文言（対象2項目）

| 項目 | 条件 | メッセージ |
|---|---|---|
| 商品名 | 未入力 | 商品名を入力してください |
| 在庫数 | マイナスの値 | 0以上の数値を入力してください |
| 発注点 | マイナスの値 | 0以上の数値を入力してください |

**判断メモ：** 在庫数・発注点はステッパー（−／＋ボタン）方式のため、UI操作だけではマイナス値を作れない。ただし25番で直接入力欄を追加したことで、キーボード入力によるマイナス値・不正値のリスクが生じるため、保険として本バリデーションを残す方針とした。また、在庫0での登録（「とりあえず登録だけ先にしておく」ケース）は正常な運用として許可し、エラー対象にはしない。

**実装・動作確認済み（2026/08/03）：** インラインエラー・上部サマリー・赤枠赤背景、いずれもClaude Designで実装・スクリーンショット確認済み。

---

## 25. 在庫数・発注点の入力方法（直接入力＋ステッパー併用）

**経緯：** ステッパー（−／＋ボタン）のみだと、大きな数値（例：100）を入力する際に100回クリックが必要になり非現実的なため、直接入力とステッパーの併用方式（案C）を採用。

### レイアウト

```
在庫数
[－]  [   24   ]  [＋]
      ↑ここは直接入力できるテキスト欄
```

- 中央の数字部分：クリックすると数値を直接入力できるテキスト欄（`inputMode="numeric"`）
- 両端の−／＋ボタン：1ずつの微調整用として残す
- 在庫数・発注点、両方に同じ構成を適用
- 入出荷記録画面の数量入力欄にも同じ方式を適用済み

### ブラウザ標準スピナー（▲▼）の非表示

`type="number"`にすると、ブラウザ標準の▲▼スピナーが自動表示され、独自の−／＋ボタンと役割が重複するため非表示にする。

```css
input[type="number"] {
  appearance: none;
  -webkit-appearance: none;
}
```

### 全角数字入力対応（28番と連動）

- IMEがひらがなモード（全角入力）のままでも、数字（０〜９）を入力できる
- 入力された全角数字は自動的に半角数字に変換する（28番参照）

### 先頭ゼロの正規化

- 入力欄からフォーカスが外れた時点（onBlur、確定操作の有無・外し方を問わずすべてのケース）で、数値として正規化する（例：「0100」→「100」）
- **重要：** 正規化した結果は、入力欄の表示（value）にも即座に反映すること。保存用の内部データだけが正規化され、画面表示が「0100」のまま更新されない、という表示と実データの不一致が過去に発生したため、同じ状態変数を参照する実装にすること

### バリデーションとの関係（24番と連動）

- フォーカスアウト時：マイナスの値や数値以外が入っていたら「0以上の数値を入力してください」を表示
- ステッパー（−ボタン）は0で止まる（0未満にはならない）実装を維持

```jsx
<div className="flex items-center gap-2">
  <button 
    type="button"
    onClick={() => setStock(Math.max(0, stock - 1))}
    className="w-11 h-11 border rounded-lg hover:border-blue-500 flex items-center justify-center"
  >
    −
  </button>
  <input
    type="number"
    inputMode="numeric"
    min="0"
    autoComplete="off"
    autoCorrect="off"
    autoCapitalize="off"
    spellCheck="false"
    value={stock}
    onChange={(e) => setStock(e.target.value)}
    onBlur={() => {
      const normalized = normalizeNumber(stock); // 全角→半角、先頭ゼロ除去
      setStock(normalized); // 表示にも反映
      validateField("stock", normalized);
    }}
    className={`w-[130px] h-[59px] text-center text-2xl font-bold border rounded-lg ${
      errors.stock ? "border-red-500 bg-red-50" : "border-gray-300"
    }`}
  />
  <button 
    type="button"
    onClick={() => setStock(stock + 1)}
    className="w-11 h-11 border rounded-lg hover:border-blue-500 flex items-center justify-center"
  >
    ＋
  </button>
</div>
{errors.stock && <p className="text-sm text-red-600 mt-1">0以上の数値を入力してください</p>}
```

**実機確認済みサイズ（2026/08/03）：** 新規登録・編集画面の数字ボックスは130px×59px（文字サイズ24px想定）。入出荷記録画面は文字サイズ44pxのため、ボックス横幅を別途拡張（29番参照）。

**実装・動作確認済み（2026/08/03）：** ▲▼スピナー非表示、先頭ゼロ正規化（半角・全角とも）、レイアウト崩れ修正、いずれもClaude Designで実装・確認済み。

---

## 26. 商品未確定データのリセット（登録フォームの不具合修正）

**経緯：** 商品登録フォームで「キャンセル」を押した後、再度「＋新しい商品を登録」を開くと前回の入力内容が残ってしまう不具合、および商品写真がテスト時にアップロードした画像のまま全商品の新規登録・編集画面にデフォルト表示されてしまう不具合が見つかり、修正した。

- 新規登録フォームを開くたびに、すべての入力欄を初期状態（空欄・在庫数0・発注点0・写真未設定）にリセットする
- 編集画面の場合は、その商品自身の元データ（商品名・カテゴリ・在庫数・発注点・単位・写真）で初期化する。新規登録と編集で挙動を分ける
- 商品の写真は、新規登録時は常に空から開始。編集時はその商品自身にアップロードされた写真のみを表示し、他の商品の写真と混ざらないようにする

**実装・動作確認済み（2026/08/03）。**

---

## 27. 番号付きページネーション（20番・23-1を更新・統合）

**経緯：** 当初のシンプルな「← 戻る／次へ →」から、商品数・ページ数の多さを考慮し、番号付きページネーションに拡張した。トップページ（20番）・商品詳細ページの前後ナビゲーション（23-1）は、いずれも同じ番号付きページネーションとして扱い、区別せず共通仕様とする。

### 構成要素

```
‹ 前へ    1 … 3 4 [5] 6 7 … 100    次へ ›
```

- **前へ**：1つ前のページに移動（先頭ページではdisabled）
- **番号リンク**：現在ページを太字・色付きで強調。現在ページの前後2つずつを表示
- **省略記号（…）**：現在ページの前後2つの範囲に収まらない番号を省略。ただし全ページ数が9ページ以下の場合は省略せず全番号表示
- **次へ**：1つ次のページに移動（最終ページではdisabled）
- **「最初へ」「最後へ」ボタンは設置しない**：省略表示のロジック上、番号「1」と最終ページ番号は常に表示され続けるため、これらをクリックすれば同じ操作ができる。ボタン数を減らし、12番の「入力項目を最小限に」の方針にも合わせた

### 表示ロジック（例）

```jsx
function getPageNumbers(current, total) {
  if (total <= 9) {
    return Array.from({ length: total }, (_, i) => i + 1); // 全部表示
  }
  const pages = new Set([1, total]);
  for (let i = current - 2; i <= current + 2; i++) {
    if (i >= 1 && i <= total) pages.add(i);
  }
  return Array.from(pages).sort((a, b) => a - b);
  // 表示時、連番でない箇所に "…" を挿入する
}
```

### 配置：ひとまとまりの帯として中央寄せ

**経緯・訂正：** 当初「前へ／次へを画面の左右端に離す」方針にしていたが、番号付きページネーションになったことで、楽天市場等の一般的なECサイトのパターンを参考に見直した。「前へ」「番号群」「次へ」を画面の両端まで引き離すのは一般的なパターンではなく、実際に実装して確認したところも中央寄せの方が自然で見やすいと判断。**トップページ・商品詳細ページとも、「前へ」「番号群」「次へ」をひとまとまりの帯として中央寄せに統一する。**

- 帯の中の要素間隔：「前へ」と数字群の間、数字群と「次へ」の間に、窮屈に見えない程度の余白を追加
- 商品詳細ページの23-1も、番号付きページネーションと同じ構成になっているため、この27番の仕様に統合（トップページと区別しない）

### 固定表示

- トップページ・商品詳細ページとも、ページネーション全体を画面下部に固定表示（sticky/fixed）にする
- 固定表示にする際、下に隠れてしまうコンテンツ（商品カードの最終行など）が隠れないよう、ページ下部に十分な余白（padding-bottom）を追加する

### フォーカス表示

- 18-22の`:focus-visible`ルールを、ページ番号リンク・前へ／次へボタンにも適用

**実装・動作確認済み（2026/08/03）：** 番号付き表示・省略記号・余白調整・中央寄せへの修正、いずれもClaude Designで実装・スクリーンショット確認済み。

---

## 28. 数字入力欄の全角対応

**経緯：** IMEがひらがなモード（全角入力）のままだと、在庫数・発注点・入出荷数量の入力欄に数字が打てない不具合が見つかった。想定ユーザー（PC操作が苦手な人・高齢者）は、IMEをひらがなモードのまま数字を打つことも多いため、対応が必要と判断した。

- IMEがひらがなモードのままでも、全角数字（０〜９）を入力できるようにする
- 入力された全角数字は自動的に半角数字に変換する（例：「１０００」→「1000」）
- 先頭ゼロの正規化（25番）は、全角・半角どちらの入力経路でも同様に適用する

**実装・動作確認済み（2026/08/03）：** 全角入力→半角自動変換、フォーカスアウト時の先頭ゼロ除去、いずれも確認済み。

---

## 29. 入出荷記録画面の数量入力ボックス幅調整（22番を更新）

**経緯：** 新規登録・編集フォームは数字の文字サイズが24pxで9桁まで表示できるが、入出荷記録画面は「現在の在庫」表示強調のため文字サイズが44pxになっており、同じボックス幅（130px）だと6桁目が見切れる問題が見つかった。

- 文字サイズ（44px）は維持する（高齢者・PC操作が苦手な人にとって大きい文字の方が見やすいメリットを優先）
- ボックスの横幅を、6桁の数字が見切れずに収まる幅まで広げる

**実装・動作確認済み（2026/08/03）。**

---

## 30. 固定ヘッダー（「戻る」系リンク含む）のsticky top化（1番を更新）

**経緯：** 商品詳細ページ・登録フォームなど、画面上部にある「一覧へ戻る」「戻る」リンクがスクロールすると画面外に消えてしまい、使い勝手が悪いという指摘があった。27番（ページネーションの下部固定）と対になる形で、画面の上下それぞれに固定要素を設ける構成とした。

- ヘッダー部分（アプリ名・店舗名の行＋「戻る」リンクの行を含むブロック全体）を、画面上部に固定表示（`position: sticky; top: 0;`）にする
- 「戻る」リンクの位置は、ヘッダーのアプリアイコンと左端を揃える（左寄りに調整）
- 対象画面：商品詳細ページ・商品登録／編集フォーム・入出荷記録画面・店舗情報画面など、「戻る」系リンクがあるすべての画面

### 重なり不具合の修正

**経緯：** ヘッダーをsticky/fixedにした際、直下のコンテンツ（カテゴリ・在庫表示ボックスなど）と重なって表示される不具合が発生した。

- 原因：ヘッダーの高さ分の余白（padding-top）が、直下のコンテンツ側に確保されていなかったため
- 修正：ヘッダーの実際の高さを測定し、直下のコンテンツのコンテナに同じ、またはそれ以上の`padding-top`（または`margin-top`）を設定する
- `position: fixed`の場合は特に、通常の文書の流れから外れるため、後ろのコンテンツに自動で余白ができない点に注意。手動での余白確保が必須

**実装・動作確認済み（2026/08/03）：** ヘッダー全体のsticky top化・「戻る」位置調整・重なり不具合の修正、いずれもClaude Designで実装・スクリーンショット確認済み。商品詳細・登録フォーム・入出荷記録・店舗情報など、対象画面すべてで同じ見た目になっていることを確認済み。

---

## 31. トップページ商品カードの再圧縮・ステータス表示のアイコン化（13-2・14-2・18-5・18-17を更新）

MacBook Air実機で「デフォルト5列は箱が大きすぎる」というフィードバックを受け、カード幅を170px固定まで圧縮し、あわせてステータス表示・ボタン表記を見直した。

### 31-1. カード幅のさらなる圧縮（13-2を更新）

**経緯：** `minmax(200px, 220px)`（13-2）で運用していたが、MacBook Air（約1470px幅）ではデフォルト5列にしかならず、iMac（約2240px幅）の8〜9列と比べて「箱が大きすぎる」という体感差があった。`auto-fit`グリッドは列数が少ないほど余った横幅を少ない列数で分け合う（＝1枚あたりが上限近くまで間延びする）ため、画面が狭い環境ほどこの間延びが目立ちやすいことが原因と判明。

対応として、`minmax`の上限・下限を同一値にして「幅を固定・列数だけが可変」の形に変更し、180px→170pxの順に段階的に圧縮して実機確認した。

```css
/* Before（13-2） */
grid-template-columns: repeat(auto-fit, minmax(200px, 220px));

/* After（31-1で確定） */
grid-template-columns: repeat(auto-fit, minmax(170px, 170px));
```

**実機確認済みの列数（2026/08/04）：**

| 環境 | 180px時 | 170px時（確定） |
|---|---|---|
| iMac（デフォルト・約2240px） | 9列 | 10列 |
| MacBook Air（デフォルト・約1470px） | 6列 | 7列 |

- MacBook Airの7列は、当初の目標だった「iMacのスペース拡大時と同程度の密度」に近づいたと判断し、170px固定で確定
- カード内の文字・アイコン・カテゴリ名（truncate表示）とも170px幅で崩れなし確認済み

### 31-2. ステータス表示をテキストバッジ→アイコンのみに変更（14-2・18-5を更新）

**経緯：** 170px幅にしたところ、「在庫あり」「⚠ 残りわずか」「⚠ 在庫切れ」のテキストバッジがカード右端からはみ出す問題が発生。フォントサイズを縮小する案もあったが、「文字ではなく視覚的に一目で分かる方が良いのでは」という発想から、テキストバッジを廃止しアイコンのみの表示に変更した。

- **在庫あり（正常時）**：バッジ自体を非表示（18-14の「正常は目立たせない」方針をさらに徹底）
- **残りわずか**：⚠️アイコンのみ（オレンジ）
- **在庫切れ**：✕アイコンのみ（赤）
- アイコンの表示位置：**商品名の右側**（在庫数の右側だと「在庫数に対するマークなのか」と誤解されるため。商品全体の状態を示すマークであることを明確にする目的）
- アイコンサイズ：14-2時点より一回り拡大（視認性向上のため）
- 在庫切れカードの背景・枠線（18-23）は変更なし、色をやや強調（`#FEE2E2` / `#EF4444`相当）

```jsx
// After（イメージ）
<div className="flex items-center gap-1">
  <span className="truncate">{name}</span>
  {status === "low" && <StatusIcon type="warning" />}
  {status === "out" && <StatusIcon type="cross" />}
</div>
<div>在庫 {stock}{unit}</div>
```

### 31-3. ツールチップ（ホバー説明）の実装方式変更

**経緯：** 当初`title`属性でアイコンの説明文（「残りわずか」「在庫切れ」）を表示していたが、ブラウザ標準の`title`は表示までに約1.5秒の遅延があり、体感で「遅い」という指摘があった。CSSベースの独自ツールチップに変更した。

- **表示方式**：CSSの`:hover`＋`opacity`transitionで実装、ホバーから0.15秒程度で表示
- **表示範囲**：アイコン単体だけでなく、**カード全体のどこにホバーしても**同じ説明文が表示される（アイコンが小さいため、狙わなくても情報が見られるように）
- **デザイン**：初期は黒背景・白文字のシンプルな吹き出しだったが、味気ないという指摘を受けて暖色系に変更
  - 残りわずか：背景`#FFF7ED`・枠線`#FED7AA`（オレンジ系）
  - 在庫切れ：背景`#FEF2F2`・枠線`#FECACA`（赤系）
  - 文字色：`#333333`（本文と同じ）、角丸`rounded-lg`程度、影`shadow-md`程度
  - 吹き出しの表示位置：アイコンの真上ではなく、カテゴリ表示（「文房具」等）と重ならない位置に調整済み

**商品名の全文ツールチップ（10番）との関係：** 商品名が省略（`truncate`）されている時のみ、商品名テキスト部分にピンポイントでホバーすると、ブラウザ標準の`title`属性で全文が表示される（従来通りの仕組みを維持、遅延あり）。カード全体ホバーのステータス説明とは別仕組みのため、ごく稀に両方が近いタイミングで表示されることがあるが、実用上の支障はないと判断（2026/08/04確認）。

### 31-4. 「見る ▶」→「›」のみに変更（18-17を更新）

**経緯：** 「▶」（三角形）が動画の再生ボタンのように見えるという指摘があり、店舗リンク（18-15）で使っている「›」（シェブロン）に統一した。

```jsx
// Before（18-17）
<button className="flex items-center justify-center gap-1">
  <span aria-hidden="true">▶</span>
  見る
</button>

// After（31-4で確定）
<div className="flex items-center justify-center" aria-hidden="true">
  <span>›</span>
</div>
```

- 18-21（カード全体クリック化）以降、この部分はすでに機能を持たない装飾表示になっていたため、文言をなくしても操作上の影響はない
- ボタン枠自体はカード最下部のまま変更なし。枠の中で「›」が上下中央に配置されていることを実機確認済み（2026/08/04、当初「ズレている」という指摘があったが、拡大表示で確認したところ問題なしと判断）

### 31-5. ページネーション列数バグの修正（20番を更新）

**経緯：** 実際には10列で描画されているのに、1ページの表示件数が24件（＝8列×3行相当）になっている不具合が見つかった。`ResizeObserver`で測定する列数と、実際にグリッドが描画する列数が同期していなかったことが原因（画面幅変更直後の古い測定値を使っていた可能性）。実際の描画列数を正しく参照するよう修正し、10列×3行＝30件が1ページに収まることを確認した。

### 31-6. 判断メモ：ステータスアイコンに発注点・現在庫の詳細は表示しない

チャッピーから「⚠️ホバー時に『在庫少／現在庫：3／発注点：5』のような詳細を出すと親切では」という提案があったが、18-6（発注目安はカードから削除し詳細画面のみに表示）の方針を維持し、採用しなかった。ツールチップの文言は「残りわずか」「在庫切れ」という状態名のみで統一。

### 31-7. 保留・次回検討事項

- **「その他」カテゴリカードの見分けにくさ**：テストデータ（テスト1〜17等）がすべて同じ📦アイコンで並ぶと見分けがつかない。実データ運用では複数商品が同じカテゴリでも商品名で判別できるため実害は小さいと考えられるが、必要であれば「その他」内でも背景色を数パターン用意するなどの対応を将来検討
- **現在の状況パネルの件数バッジをクリック可能にする案**（チャッピー提案）：クリックすると自動でその状態のフィルターがかかる案。便利だが、18-11で確立した「非クリック要素として明示する」方針と矛盾するため、方針変更が必要になる。今回は採用せず保留

---

### 31-8. 列数確認時の注意点（2026/08/07の調査より）

MacBook AirとiMacで列数を比較した際、一時的に「画面が狭いはずのiMacの方が列数が少ない」という逆転現象が起きたが、調査の結果バグではなく、以下2点が原因と判明した。

- **商品件数が揃っていないと、`auto-fit`が実際の画面幅より少ない列しか描画しない**（件数が少ないと使われない列トラック自体が生成されないため）。列数を比較する際は、両端末で同じ商品件数に揃える必要がある（「テストデータを読み込む」ボタンで揃えると楽）
- **ブラウザの表示倍率（ズーム）を100%に揃える必要がある**。ズームが100%でない状態だと`window.innerWidth`が実際の画面サイズと一致しないため、正しい比較ができない（`Cmd + 0`でリセット）

**検証済みの正しい結果（2026/08/07、件数・ズームを揃えた状態）：**

| 端末 | `window.innerWidth` | 列数 |
|---|---|---|
| iMac | 1989px | 11列 |
| MacBook Air | 1219px | 7列 |

画面が広いほど列数が多いという、想定通りの挙動を確認。以降、列数を比較・確認する際は「①商品件数を揃える」「②ズームを100%にする」の2点をまず確認すること。

---

## 32. Canva連携の試行結果（不採用・保留の記録）

Claude Design以外のツールでの画面制作・アイコン制作を試す目的で、Canva（MCP連携）を試行した。結果は以下の通り。

### 32-1. 画面全体（トップページ・商品詳細・商品登録）の再現 → 不採用

- Apple／Notion／Linear／Stripe風の洗練されたSaaSデザインを目指してCanva AIに一括生成させたが、文字崩れ・整列のガタつきが発生
- **結論：** 「情報量が多いWebアプリ画面全体」の一括生成は、Canva AIが苦手とする領域と判明
- 画面全体の設計・確認は、引き続き**Claude Designが担当**する
- Canva・Figmaの位置づけ：Canva／Figmaは「アイコン単体などパーツ素材」向き。Figma自体は本来「デザインシステムを厳密に作って実装に渡す」用途に強いツールであり、今回のような1人開発・LocalStorage構成のポートフォリオでは、Claude Designで完結させる方が効率的という棲み分けを確認

### 32-2. アイコン素材のCanva試作（検討・保留）

先生の勧めでCanva連携を試すため、絵文字アイコン（2番・6番・7番・18-10）の置き換え候補として、イラスト風アイコンセット（8カテゴリ＋在庫警告2種）をCanva AIで試作した。

**結果：**

- 1枚のシートに複数アイコンを一括生成する方式では、絵柄とラベルがズレる問題が発生（例：「飲み物」の位置にペット用品風の肉球アイコンが出る、「衣類」と「美容・コスメ」の絵柄が入れ替わるなど）
- 精度を出すには1アイコンずつ個別生成する必要があり、10個分の時間的コストが見合わないと判断
- **結論：現時点では絵文字方式（2番・6番・7番・18-10）を継続。本番アイコンへの置き換えは保留**
- きっかけは「新しいツールを試してみたい」という好奇心ベースであり、絵文字方式に機能的な問題があったわけではないため、無理に置き換える必要性は低いと判断

---

## 33. PC幅での2カラムレイアウト（登録フォーム・商品詳細ページ）

**経緯：** 1024px以上のPC画面で、登録・編集フォームや商品詳細ページが縦一列のままだと、横の余白を活かしきれていなかったため、PC幅限定で2カラムレイアウトに変更した。

### 対象範囲

- 適用：1024px以上（`lg:`ブレークポイント）
- 非適用：1024px未満（タブレット・スマホ幅）は従来通りの縦並びレイアウトを維持

### 実装方式

**当初案（不採用）：** `flex flex-col lg:flex-row`
→ モバイル時にヒーローエリアの崩れ・折り返し問題が発生したため不採用。

**採用案：** `grid grid-cols-[7fr_3fr]` ＋ `lg:contents`

```jsx
<div className="grid grid-cols-1 lg:grid-cols-[7fr_3fr] gap-6">
  <div className="lg:contents">
    {/* 左カラム相当の要素 */}
  </div>
  <div className="lg:contents">
    {/* 右カラム相当の要素 */}
  </div>
</div>
```

- `lg:contents`を使うことで、1024px未満では要素がグリッド構造に縛られず自然に縦積みされ、1024px以上でのみ7:3のカラム分割が有効になる

### 登録・編集フォーム（ItemForm.jsx）

- 左70%：商品名／カテゴリ／在庫数／発注点／単位
- 右30%：写真アップロード＋キャンセル／保存するボタン
- コンテナ最大幅：`lg:max-w-[880px]`（34番で960pxに更新）

### 商品詳細ページ（ItemDetail.jsx）

- 左70%：商品名／カテゴリ／在庫ボックス／発注点／単位／更新日時（新規追加）／入出荷履歴
- 右30%：商品画像＋ボタン群
- コンテナ最大幅：`lg:max-w-[960px]`

### 更新日時の新規表示

- `formatDate()`を使用し、「更新日時：yyyy/mm/dd」形式で発注点・単位の近くに表示
- データモデル（企画書5章）のItemには元々`updatedAt`が存在していたが、画面上に未表示だったため今回追加

**実装・動作確認済み（2026/08/08）：** 1024px以上での2カラム表示、1024px未満での従来縦並び維持、両方PC実機で確認済み。

---

## 34. 登録・編集フォームの細部調整（2カラム化後の見直し）

**経緯：** 33番の2カラム化により左カラムの幅が狭まったことで、既存の数値（25番の130pxボックス等）が窮屈に見える／余白バランスが崩れる問題が複数見つかったため、まとめて調整した。

### 34-1. 在庫数・発注点の数字ボックス幅を拡大（25番を更新）

```jsx
// Before（25番）
className="w-[130px] h-[59px] ..."

// After
className="w-[160px] h-[59px] ..."
```

### 34-2. 単位入力欄の幅を縮小

「個」「本」など1〜4文字しか入らないため、商品名と同じフル幅は不要と判断。

```jsx
// Before
<input value={unit} className="w-full border ..." />

// After
<input value={unit} className="w-full max-w-[160px] border ..." />
```

### 34-3. カテゴリのプルダウン選択肢を8種類に修正（実装漏れの修正）

6番で確定していた8分類（文房具／雑貨／食品／飲み物／ペット用品／衣類／美容・コスメ／その他）が、登録フォームには4種類（文房具／雑貨／食品／その他）しか反映されていなかった。

```js
// Before
const categoryOptions = ["文房具", "雑貨", "食品", "その他"];

// After（6番の8分類と統一）
const categoryOptions = ["文房具", "雑貨", "食品", "飲み物", "ペット用品", "衣類", "美容・コスメ", "その他"];
```

### 34-4. カテゴリのプルダウン幅を縮小

```jsx
// Before
<select className="w-full border ...">

// After
<select className="w-full max-w-[280px] border ...">
```

### 34-5. フォーム全体のコンテナ最大幅を拡大（33番を更新）

2カラム化直後は`lg:max-w-[880px]`で運用していたが、左右の余白がかなり広く空いてしまう問題があったため、商品詳細ページ（ItemDetail、`lg:max-w-[960px]`）と統一した。

```jsx
// Before
className="lg:max-w-[880px] mx-auto"

// After
className="lg:max-w-[960px] mx-auto"
```

### 34-6. 単位欄とキャンセル/保存するボタンを同じ行に配置

34-2で単位欄を縮小したことにより、右カラムのボタン群（キャンセル/保存する）との間に不自然な空白ができていた問題を修正。単位欄とボタン群を2カラムグリッドの下に、全幅の新しい行として配置し直した。

```jsx
// 実装イメージ
<div className="flex flex-col lg:flex-row lg:justify-between gap-4">
  <div>{/* 単位入力欄（34-2のmax-w-[160px]のまま） */}</div>
  <div className="flex gap-3">{/* キャンセル・保存するボタン */}</div>
</div>
```

- lg以上：単位＝左、ボタン群＝右で同じ行に横並び
- lg未満：縦積みに戻る（従来通り）

### 34-7. キャンセル・保存するボタンの幅を拡大

34-6でボタンを単位欄と同じ行に配置した際、ボタン自体が小さく見えバランスが悪かったため、幅のみ調整した。

- 共通コンポーネント`Button.jsx`自体は変更しない（他画面への影響を避けるため）
- `ItemForm.jsx`内のキャンセル・保存するボタンにのみ`min-w-[120px]`を追加
- 高さ（`min-h-[48px]`、Button.jsx側で定義）は変更なし

**実装・動作確認済み（2026/08/08）：** 34-1〜34-7すべて、デスクトップ・タブレット・モバイル幅でスクリーンショット確認済み。カテゴリ8種類（「美容・コスメ」含む）の選択動作もPlaywrightで確認済み。

---

## 35. 「戻る」リンクの `/items/undefined` バグ修正（30番関連）

**経緯：** 商品詳細ページから編集画面を開き、ヘッダーの「戻る」リンク（30番のsticky戻るリンク）を押すと、一覧ページに戻るはずが「商品が見つかりません」画面に一旦遷移してしまう不具合が見つかった。もう一度「一覧へ戻る」を押すと正しく遷移する。同じ編集画面内の「キャンセル」ボタンは問題なく商品詳細ページに戻っていたため、ヘッダー側固有の問題と判明。

### 原因

`Header`コンポーネントが`App.jsx`の`<Routes>`の外側（兄弟要素）にレンダリングされる構成になっていたため、Header内で使っていた`useParams()`はどのRouteにも属さず、常に空オブジェクトを返していた。これにより編集画面の「戻る」リンクが組み立てる遷移先`/items/${id}`の`id`が`undefined`のままになり、実際には`/items/undefined`に遷移して該当商品が見つからず、`ItemDetail.jsx`のフォールバック（「商品が見つかりません」画面）が一旦表示されていた。

### 修正内容

`useParams()`への依存をやめ、各`useMatch()`が返す一致結果自身の`.params.id`を参照する方式に変更。`useMatch()`はルート階層に関係なく現在のURLパスから直接パラメータを取り出せるため、Headerがルート階層の外にあっても正しく動作する。

```jsx
// Before（Routes外のためidが常にundefined）
const { id } = useParams();

// After（現在のURLパスから直接取得）
const match = useMatch("/items/:id/edit");
const id = match?.params?.id;
```

**実装・動作確認済み（2026/08/08）：** 商品詳細・登録画面・編集画面それぞれで「戻る」リンクを1回クリックし、`/items/undefined`を経由せず正しい遷移先（それぞれ `/`・`/`・`/items/1`）に直接到達すること、「商品が見つかりません」画面が表示されないことをPlaywrightで確認。

**教訓：** ルート階層の外側（Routesの兄弟要素）に配置したコンポーネントでは`useParams()`は使えない。現在のURLパスからパラメータを取りたい場合は`useMatch()`を使う。

---

## 36. 在庫数・発注点ボックスの重なりバグ修正（34-1関連）

**経緯：** 34-1で数字ボックスを130px→160pxに拡幅した後、ブラウザ幅が1024px〜1280px程度の中間幅（lg breakpoint前後）になると、発注点のラベル「発注点（下回ったら知らせます）」が2行に折り返り、それに伴って発注点のstepper（－／数字／＋）の横位置がずれ、在庫数側の「＋」ボタンと発注点側の「－」ボタンが重なって表示される不具合が見つかった。

### 原因

親要素には`flex flex-wrap gap-6`がすでに設定されていたが、在庫数・発注点の各ボックス（グループ）には`flex-1`（＝`flex-basis: 0%`）と`min-w-[220px]`が指定されていた。CSSのflexboxの折り返し判定は「flex-basisをmin-width/max-widthでクランプした値」を基準に行われるため、`flex-1`＋`min-w-[220px]`の組み合わせでは折り返り判定が220px基準になっていた。

しかし実際に必要な幅は「－／＋ボタン52px×2＋160px入力欄＋余白 ＝ 約304px」であり、220pxでは足りない。そのため本来2段に折り返るべき幅でも「1行に収まる」と誤判定され、`flex-grow`の計算で304pxに満たない幅までボックスが縮められ、中の要素（stepperや＋ボタン）がボックスからはみ出して隣のボックスと重なっていた。

### 修正内容

各グループの`flex-1`を`flex-auto`（＝`flex-basis: auto`）に変更。`flex-auto`では中身の実際のサイズを基準に折り返り判定が行われるため、正しいタイミングで2段に折り返るようになる。

```jsx
// Before（flex-basis: 0%のため折り返り判定がmin-w基準になり誤判定）
<div className="flex-1 min-w-[220px]">
  {/* 在庫数 or 発注点のラベル＋stepper */}
</div>

// After（flex-basis: autoのため中身の実サイズで折り返り判定される）
<div className="flex-auto min-w-[220px]">
  {/* 在庫数 or 発注点のラベル＋stepper */}
</div>
```

**実装・動作確認済み（2026/08/08）：** 768px／1024px／1100px／1280px／1920pxの5パターンで、Playwrightによる座標計算（overlaps: false）で重なりが発生しないことを確認。いずれの幅でも発注点ラベルの折り返り有無にかかわらず正しく表示される。

**教訓：** `flex-1`は`flex-basis: 0%`を意味し、折り返り判定がmin-width任せになるため、中身のサイズが可変（ラベルの折り返り等で高さ・レイアウトが変わる）な要素には不向き。中身の実サイズを基準に折り返らせたい場合は`flex-auto`（`flex-basis: auto`）を使う。

---

## 37. 登録・編集フォーム 2カラムレイアウトの仕上げ調整（33〜36番の実装後の見直し）

**経緯：** 33番の2カラム化を実機（PC幅）で確認したところ、複数の不具合・見た目の粗さが見つかったため、まとめて調整した。

### 37-1. 在庫数・発注点が左カラム内に収まらず縦積みになる不具合

**原因：** 在庫数・発注点のグループが左カラム（70%幅、960px時で約670px）の内側に配置されていたが、2グループ横並びには約600px超必要（36番の分析通り、1グループ約304px）。670pxではぎりぎり収まらず`flex-wrap`が働いて縦並びに折り返り、結果としてフォーム全体が縦に伸びてスクロールが発生していた。

**修正：** 在庫数・発注点のグループを7:3グリッドの外側に出し、`lg:col-span-2`（両カラムにまたがる全幅行）に変更。コンテナ全幅（960px）を使えるようになり、横並びが復活してスクロールも解消した。

### 37-2. 在庫数・発注点間のgapを縮小

`gap-6`（24px）→`gap-4`（16px）に変更。2ボックスの間が空きすぎていた見た目を調整。

### 37-3. キャンセル・保存するボタンの幅を拡大（34-7を更新）

`min-w-[120px]` → `min-w-[180px]`（約1.5倍）に変更。

### 37-4. 狭い画面幅での表示順変更（商品の写真を後ろに）

**経緯：** lg未満の画面幅で「商品名→カテゴリ→商品の写真→在庫数→発注点→単位→ボタン」という順で表示されていたが、写真は任意項目のため必須項目群より先に来るのは不自然という指摘があった。

**修正：** 各ブロックに`order-*`を付与し、lg未満では「商品名→（カテゴリ＋単位）→在庫数＋発注点→商品の写真→ボタン」の順に変更（37-6のカテゴリ・単位統合後の順序）。lg以上では従来通り写真は右カラム上部に固定。

### 37-5. 最小幅でのボタン横並び維持、および幅切り替え基準の統一

**経緯：** 画面幅が最も狭くなるとキャンセル・保存するボタンが縦に1列で積み重なっていた。また、640px（`sm:`）を基準にボタン幅を固定に切り替える実装にしたところ、フォーム自体が1カラム→2カラムに切り替わる`lg:`（1024px）とズレが生じ、640〜1023pxの間（タブレット幅）でまだ1カラムで余裕があるのにボタンだけ先に小さく固定される不具合が発生した。

**修正：**
- ボタンを囲むコンテナを`flex`（wrapなし）にし、各ボタンに`flex-1`を付与。lg未満では常に横並び・横幅いっぱいに均等分割される。
- 幅の固定切り替えは`sm:flex-none sm:min-w-[180px]`ではなく`lg:flex-none lg:min-w-[180px]`に統一。lg（1024px）未満は常に均等分割、lg以上でのみ固定幅180pxに切り替わる。

```jsx
<div className="flex gap-3">
  <button className="flex-1 lg:flex-none lg:min-w-[180px] ...">キャンセル</button>
  <button className="flex-1 lg:flex-none lg:min-w-[180px] ...">保存する</button>
</div>
```

### 37-6. カテゴリ・単位を横並びグループに統合

カテゴリと単位は元々別々の行だったが、どちらも短い入力欄のため、在庫数・発注点と同じ`flex flex-wrap gap-4`パターンで1グループにまとめた。カテゴリ`max-w-[280px]`、単位`max-w-[160px]`は維持。幅が足りない場合は自然に縦積みに折り返る（390px幅で確認済み）。単位の独立した行・orderは削除し、カテゴリのグループに統合した。

表示順（最終形）：商品名 →（カテゴリ＋単位、横並び）→（在庫数＋発注点、横並び）→ 商品の写真 → ボタン行

### 37-7. 商品の写真とボタン行の間の余白拡大

写真ブロックとボタン行は別々のgridアイテム（写真は`lg:col-start-2`、ボタンは`lg:col-start-1`→37-8で`lg:col-span-2`）のため、グローバルなgapを上げると全行の間隔が均等に広がってしまう。ボタン行のコンテナに個別で`mt-4`を追加する形で対応（既存gap-5.5に+16px）。モバイル1カラム時・デスクトップ両方で余白が広がったことを確認。

### 37-8. ボタン行を写真カラムの右端に揃える

**経緯：** lg以上の2カラム表示で、ボタン行が左カラム（70%幅）の中でのみ右寄せになっており、フォーム全体（写真カラムを含む）の右端までは届いていなかった。一般的な2カラムフォームでは「ボタンは全体の右下」に配置されることが多く、統一感に欠けていた。

**修正：** ボタン行を`lg:col-start-1`から`lg:col-span-2`（両カラムにまたがる全幅行）に変更し、内側のflexコンテナに`lg:justify-end`を追加。lg以上で保存するボタンの右端が写真ボックスの右端と一致するようになった。写真ブロックの`row-span`（37-1以前の対応で4→2に縮小済み）とは行が重ならないため、追加調整は不要だった。

**実装・動作確認済み（2026/08/08）：** 1280px／900px（または820px）／630px／390px幅で、以下を確認。
- 1280px：カテゴリ＋単位・在庫数＋発注点それぞれ横並び、ボタンが固定幅180pxで写真カラム右端に揃って配置
- 820px・630px：カテゴリ＋単位・在庫数＋発注点は横並び維持、ボタンは横幅いっぱいの均等分割（flex-1）
- 390px：カテゴリ＋単位が幅不足のため自然に縦積みに折り返り、写真がボタン直前の正しい位置に表示、ボタンは横幅いっぱいの横並びを維持
- 写真とボタン行の間の余白拡大、フォーム全体がスクロールなしで収まることを確認

---

## 38. チャッピーレビュー（登録フォーム）反映：写真枠縮小・発注点文言・保存ボタン文言

**経緯：** チャッピー（別AIツール）による登録フォームのスクリーンショットレビューで5つの改善案が出た。design-tokensの既存決定と照らし合わせ、以下の3点を採用（残り2点は見送り・対応不要と判断）。

**見送り・対応不要と判断した項目：**
- 在庫数・発注点の直接入力化：25番・18-12ですでに実装・確認済みのため対応不要（チャッピーが古い状態を見ていた可能性）
- 単位のプルダウン化：4番で「将来追加予定」と位置づけ済み。今回は見送り

**訂正（2026/08/08）：** 当初、GitHub main上の`ItemForm.jsx`を確認した際に33番・37番の内容（`gap-4`・ボタン幅`min-w-[180px]`・`lg:justify-end`）が反映されていないように見えたが、これは`raw.githubusercontent.com`側のキャッシュを参照していたための誤検知だった。くうさんに依頼してMacBook Air側で`git status`／`git log`／`git branch`／`git fetch`＋diffを確認してもらったところ、ローカルとorigin/mainは完全に同一コミット（`ca17e87`）で、33〜37番の内容はすべて正しくmainに反映済みと確認できた。キャッシュ回避（`?nocache=`パラメータ付与）で再取得し、実際に一致することも確認済み。お手数をおかけした。

以下の3項目は、キャッシュ回避後に再取得した実際のGitHub main上のコード（コミット`ca17e87`時点）をベースに記述している。

### 38-1. 写真アップロード枠の縮小（`PhotoUpload.jsx`）

現状：`h-[180px] w-full`（右カラム幅いっぱい＝実測約265px幅×180px高さ）

```jsx
// Before
className="relative flex h-[180px] w-full cursor-pointer items-center justify-center overflow-hidden rounded-2xl border-2 border-dashed"

// After
className="relative flex h-[150px] w-full max-w-[220px] cursor-pointer items-center justify-center overflow-hidden rounded-2xl border-2 border-dashed"
```

- 高さ180px→150px、幅に上限220pxを追加（現状比どちらも約15〜20%減）
- 理由：チャッピーによるiPad Pro表示のレビューで「写真枠の存在感が大きい」との指摘。写真は任意項目であり、フォームの主役は商品情報のため、写真枠は「分かりやすさを保ちつつ少し控えめに」する方針
- ドラッグ&ドロップの案内文（「写真をドラッグ＆ドロップ／またはクリックして選択」の2行）は縮小後のサイズでも収まる想定。実装後に実機で折り返し崩れがないか要確認

### 38-2. 発注点ラベルの文言修正（`ItemForm.jsx`）

判定ロジック自体（`stock === 0 || stock <= threshold`、14-2で確定済み）は変更しない。表示文言をロジックの実態（「以下」で警告）に合わせる。

```jsx
// Before
<label className="mb-2 block text-[15px] font-bold">発注点（下回ったら知らせます）</label>

// After
<label className="mb-2 block text-[15px] font-bold">発注点（ここ以下になったら知らせます）</label>
```

**関連：** 企画書7章の「発注点未満の商品を赤色表示」という表現も、実装（以下で警告）と不一致なため「発注点以下の商品を赤色表示」に修正するのが望ましい（企画書側の修正はdocx編集が必要なため、別途対応）。

### 38-3. 保存ボタンの文言（新規登録時のみ変更）

編集画面のボタン文言（「保存する」「保存しています...」）は変更しない。新規登録時のみ、より具体的な文言にする。

```jsx
// Before
<Button
  variant="primary"
  loading={saveLoading}
  onClick={handleSubmit}
  className="min-w-0 flex-1 lg:flex-none lg:min-w-[180px]"
>
  {saveLoading ? "保存しています..." : "保存する"}
</Button>

// After
<Button
  variant="primary"
  loading={saveLoading}
  onClick={handleSubmit}
  className="min-w-0 flex-1 lg:flex-none lg:min-w-[180px]"
>
  {saveLoading
    ? (isEdit ? "保存しています..." : "登録しています...")
    : (isEdit ? "保存する" : "商品を登録する")}
</Button>
```

- `className`（37-5でのボタン幅対応）は変更しない。文言部分（三項演算子）のみ差し替える
- `isEdit`はコンポーネント冒頭で既に定義済みの変数をそのまま利用
- キャンセルボタンの文言・className・共通コンポーネント`Button.jsx`自体は変更なし

**ステータス：** 3項目とも設計決定・記録・実装・確認済み（コミット`c98e762`、2026/08/08）。iPad Air（820px）・iPad Pro（1024px）・iPad mini（768px）・MacBook Air（実機フル画面）で目視確認済み。写真枠の縮小後もドラッグ&ドロップの案内文が2行で問題なく収まること、発注点ラベルの新しい文言、保存ボタンの新規登録時「商品を登録する」／編集時「保存する」の出し分けを確認。lintはエラーなし（既存の無関係な警告2件のみ）。

---

## 39. タブレット幅（iPad系）でのバランス調整（38番の副作用対応）

**経緯：** 38番の実装確認（iPad Air／iPad Pro／iPad mini／MacBook Airの4サイズ）で、チャッピーの指摘には無かった新たな見た目のバランス崩れが3件見つかった。

### 39-1. コンテナ最大幅のタブレット対応（33番・34-5を更新）

**経緯：** コンテナ最大幅が`max-w-[640px]`（lg未満）→`lg:max-w-[960px]`（1024px以上）の2段階しかなく、768〜1023px（iPad mini・iPad Air相当）では640px制限のままのため、画面幅に対して不釣り合いに大きな左右余白ができていた。

```jsx
// Before（ItemForm.jsx・ItemDetail.jsx共通のコンテナ）
<div className="mx-auto max-w-[640px] px-6 py-5 lg:max-w-[960px]">

// After
<div className="mx-auto max-w-[640px] px-6 py-5 md:max-w-[760px] lg:max-w-[960px]">
```

- `md:`（768px以上）で760pxに拡大する段階を追加。768〜1023pxの幅で余白が自然になる
- ItemForm.jsxだけでなく、同じコンテナパターンを使っているItemDetail.jsxにも同様の対応が必要か確認すること

### 39-2. キャンセル・保存するボタンの間隔を拡大

```jsx
// Before
<div className="flex gap-3 lg:justify-end">

// After
<div className="flex gap-4 lg:justify-end">
```

- `gap-3`（12px）→`gap-4`（16px）。他の横並びグループ（カテゴリ＋単位、在庫数＋発注点）と同じ`gap-4`に統一する意味もある

### 39-3. 写真ブロックの縦方向の余白調整（38-1の副作用）

**経緯：** 38-1で写真枠を180px→150pxに縮小した結果、右カラム（`lg:row-span-2`で商品名＋カテゴリ行の高さ分を確保）に対して写真が小さくなりすぎ、下に不自然な空白ができた。

**方針：** `row-span-2`自体は33番の左右バランス（商品名＋カテゴリ行の高さと写真の高さを揃える）のために必要なので維持しつつ、写真ブロックをそのスパン内で縦方向に中央寄せし、空白を「上下に分散」させることで「下だけに余っている」不自然さを解消する。

```jsx
// Before
<div className="order-4 lg:order-1 lg:col-start-2 lg:row-span-2">
  <label className="mb-2 block text-[15px] font-bold">商品の写真（任意）</label>
  <PhotoUpload ... />
</div>

// After（縦方向中央寄せを試す）
<div className="order-4 lg:order-1 lg:col-start-2 lg:row-span-2 lg:self-center">
  <label className="mb-2 block text-[15px] font-bold">商品の写真（任意）</label>
  <PhotoUpload ... />
</div>
```

- **要実機確認：** `lg:self-center`で改善しない場合は、`lg:row-span-2`自体を外して自然な高さに任せる代替案も検討する（ただしその場合、商品名・カテゴリ行との左右の高さ揃えが崩れる可能性があるため、まず`self-center`案から試す）

### 39-4. 2カラム化の基準（1024px→1280px への引き上げ）について

iPad Pro（1024px）で2カラム化した際の「窮屈さ」は、主に39-3の空白問題が原因だった可能性が高いため、今回は39-1〜39-3の対応を優先し、2カラム化の基準（`lg:`＝1024px）自体の変更は一旦保留とする。39-1〜39-3を実装・確認したうえで、まだ窮屈に感じる場合に改めて検討する。

**ステータス：** 設計決定・記録・実装・確認済み（2026/08/08）。iPad mini（768px）・iPad Air（820px）・iPad Pro（1024px）・MacBook Air（1280px）の4サイズで確認。

- 39-1（コンテナ幅md:対応）：iPad mini・iPad Airで左右の余白が改善、横スクロールなし
- 39-2（ボタンgap拡大）：実装済み
- 39-3（写真ブロック`lg:self-center`）：iPad Proで下の空白が解消。写真ブロック高さ（約180px）と左カラム2行分の高さ（約170px）がほぼ一致していたため、`lg:row-span-2`を外す代替案は不要だった
- ItemDetail.jsxにも同じコンテナパターンがあったため、`md:max-w-[760px]`を同様に追加（ボタンgap・写真self-centerはItemForm.jsx固有のため対象外）

**追加の確認・判断（39-5）：白いカードの下、ページ全体の余白について**

39-1〜39-3の対応後、iPad mini／iPad Air／iPad Proで「白い枠線カードの下、ページ全体（薄いクリーム色の背景部分）に余白が残る」という指摘があった。調査の結果、この余白は画面の**高さ**（768→820→1024pxではなく、1024→1180→1366pxという高さの違い）にほぼ比例しており、カード内部の空白ではなく、フォームをページ上部に寄せて配置する一般的なWebフォームの挙動であることを確認した。

**判断：** 対応不要・現状のままとする（選択肢A採用）。

- 12番「余白を広めに取る」というアクセシビリティ方針とも一貫性がある
- カードを画面縦中央に配置する代替案（選択肢B）も検討したが、24番のバリデーションエラー表示でカード高さが変わった際に中央位置がズレる・sticky headerとの兼ね合いで実装が複雑になる、というトレードオフがあり見送った
- プレゼンで問われた場合の説明案：「余白を広めに取り、視線誘導を意識したアクセシビリティ重視の設計」（12-2と整合）

---

## 40. 入出荷記録画面・店舗情報画面へのhover明示・処理中表示の展開（22番の適用）

**経緯：** 22番で確立した「画面遷移のみ vs 実データ書き込みあり」の分類基準を、入出荷記録画面（`StockRecord.jsx`）・店舗情報画面（`Shop.jsx`）にも展開した。

### 40-1. ボタン構成の確認

両画面とも、実装時点でキャンセルボタンは存在せず、画面遷移はヘッダーの「戻る」リンクのみだった。「戻る」リンクは処理中でも押下可能な単純な画面遷移であり、データ書き込みと連動していないためdisabled化の対象外と判断（12番「入力項目・操作を最小限に」の方針とも整合するため、新規にキャンセルボタンを追加する対応は見送り）。

### 40-2. StockRecord.jsx（入出荷記録画面）

「記録する」ボタン（実データ書き込みあり＝在庫更新＋履歴追加）に`loading`状態を追加。

```jsx
<Button variant="primary" loading={recordLoading} onClick={handleRecord}>
  {recordLoading ? "記録しています..." : "記録する"}
</Button>
```

### 40-3. Shop.jsx（店舗情報画面）

「保存する」ボタン（実データ書き込みあり＝店舗名・住所・電話番号の更新）に同様の対応。

```jsx
<Button variant="primary" loading={saveLoading} onClick={handleSave}>
  {saveLoading ? "保存しています..." : "保存する"}
</Button>
```

**実装・動作確認済み（2026/08/08、コミット`f2023ba`）：** Playwrightによるブラウザ検証で、両画面とも押下中のテキスト切り替え（「記録しています...」「保存しています...」）・disabled化・完了後の画面遷移（入出荷記録→商品詳細／店舗情報→トップページ）を確認済み。lintエラーなし。

---

## 41. カテゴリアイコンの絵文字方式への復元（2番・6番の実装漏れ修正）

**症状：** トップページの商品カードで、カテゴリアイコンが本来の絵文字（✏️🎁🍙☕🐾👕🧴📦）ではなく手描きのSVGアイコンになっており、しかも`文房具`／`雑貨`／`食品`／`その他`の4種類分しか実装されていなかった。34-3でプルダウンの選択肢は8分類に修正済みだったが、アイコン側（`CategoryIcon.jsx`）は取り残されており、`飲み物`・`ペット用品`・`衣類`・`美容・コスメ`の商品はすべて「その他」用の汎用アイコンで表示されていた。

とくに「食品」のSVGアイコン（三角形の輪郭）が、31-2で定義した在庫少バッジの⚠️アイコンと見た目が近く、「在庫少の商品はカテゴリ絵文字ではなく警告アイコンが出ている」ように見える紛らわしさがあった。

**原因：** 絵文字方式（2番）からSVGアイコンセットへの置き換えは、末尾の「未決定・次回検討事項」に`[ ] categoryStyle.jsの絵文字アイコンを、将来的にはSVGアイコンセットに置き換えるか`として保留のまま残っていた項目であり、正式決定前に一部だけ実装が先行してしまっていたと判明。

**対応：** `components/CategoryIcon.jsx`の`CATEGORY_META`を、6番の8分類マッピングに合わせて絵文字ベースに戻し、欠けていた4分類を追加した。

```js
// After（41番で確定、6番の8分類と統一）
const CATEGORY_META = {
  文房具: { bg: "oklch(0.94 0.04 235)", icon: "✏️" },
  雑貨: { bg: "oklch(0.94 0.035 300)", icon: "🎁" },
  食品: { bg: "oklch(0.94 0.05 55)", icon: "🍙" },
  飲み物: { bg: "oklch(0.94 0.04 200)", icon: "☕" },
  ペット用品: { bg: "oklch(0.94 0.04 140)", icon: "🐾" },
  衣類: { bg: "oklch(0.94 0.04 20)", icon: "👕" },
  "美容・コスメ": { bg: "oklch(0.94 0.04 350)", icon: "🧴" },
};
const DEFAULT_META = { bg: "oklch(0.94 0.01 260)", icon: "📦" }; // その他
```

- 背景色は既存の3分類（文房具・雑貨・食品）の`oklch`配色パターンを踏襲し、新規4分類にも色相違いの背景を割り当てた（Tailwindの`bg-blue-50`等ではなく、既存実装に合わせてインラインの`oklch`値を使用）
- 併せて、ステータスアイコン（31-2、⚠️/✕）がカテゴリアイコンの表示枠を奪っていないか`ItemCard.jsx`を確認したが、両者はもともと別要素（カテゴリアイコンは上段の丸枠内、ステータスアイコンは商品名の右側）として実装されており、31-2の方針通りだった。今回の見た目の混乱は、カテゴリアイコン側の絵文字未実装・SVGへの誤った先行実装が原因であり、ステータスアイコン側の実装に問題はなかった。

**実装・動作確認済み（2026/08/08）：** Playwrightで8分類すべての商品カード（在庫あり・在庫少・在庫切れを混在）をスクリーンショット確認し、カテゴリごとに正しい絵文字が表示され、在庫少・在庫切れの商品でもカテゴリ絵文字はそのまま維持されつつステータスマークが商品名右側に独立して表示されることを確認済み。lintエラーなし（既存の2件の未使用警告のみ、本修正とは無関係）。

---

## 42. 入出荷履歴の行レイアウト：flex両端揃え→グリッド固定幅化

**経緯：** 各行が`flex justify-between`で組まれていたため、画面幅が広いPCほど「日付・バッジ」と「メモ・数量」の間の空白が不自然に間延びして見える問題があった。

```jsx
// Before
<div className="flex items-center justify-between ...">
  <div className="flex items-center gap-3">{/* 日付・バッジ */}</div>
  <div className="flex items-center gap-3">{/* メモ・数量 */}</div>
</div>

// After（sm以上、42番で確定）
<div className="grid grid-cols-[160px_100px_1fr_80px] items-center gap-4 ...">
  <span>{date}</span>
  <Badge />
  <span className="min-w-0 truncate">{memo}</span>
  <span className="text-right">{quantity}</span>
</div>
```

- 日付・バッジ・数量列は固定幅、メモ列のみ`1fr`で可変（長文は`truncate`で省略、`min-w-0`がないと列が縮まずtruncateが効かない点に注意）
- メモが空文字の行でも、要素自体は必ずレンダリングして4列の位置を維持（条件付きレンダリングで要素ごと消すと、数量が3列目にずれてグリッドが崩れるため）
- **sm未満（モバイル）は固定幅のままだと列合計＋ギャップが画面幅を超えて横スクロールが発生したため**、`grid-cols-[auto_auto_1fr_auto] gap-2`（内容に応じた可変幅）に変更し、`sm:`以上でのみ固定幅`[160px_100px_1fr_80px] gap-4`を適用するレスポンシブ対応にした
- 右側の固定フローティングボタン（39番の↑ボタン）との重なり回避のため`pr-14`（モバイル〜タブレット）を維持しつつ、右カラムが元々ボタンから離れているlg以上では`lg:pr-1`（従来のpx-1相当）に戻す

**実装・動作確認済み（2026/08/08）：** Playwrightでデスクトップ（1280px）・タブレット（820px）・モバイル（390px）の3幅を確認。長文メモの省略表示、メモなし行でのレイアウト崩れなし、モバイル幅での横スクロール発生なし（対応前は発生していたためレスポンシブ化が必須だった）、↑ボタンと数量列の重なりなしをすべて確認済み。lintエラーなし。

## 42-1. 入出荷履歴：メモ折り返し表示＋上揃えへの変更（42番の見直し）

**経緯：** 42番では長文メモを`truncate`で1行省略表示にしていたが、メモ内容を省略せず全文確認したいという要望により、折り返し表示（複数行可）に変更。それに伴い、行全体が`items-center`のままだとメモが複数行になった際に日付・種別バッジ・数量が行の中央に来てしまい不自然なため、`items-start`（上揃え）に変更。

```jsx
// Before（42番）
<div className="grid grid-cols-[auto_auto_1fr_auto] items-center ...">
  <span className="min-w-0 truncate text-[13px]">{memo}</span>
</div>

// After（42-1番で確定）
<div className="grid grid-cols-[auto_auto_1fr_auto] items-start ...">
  <span className="min-w-0 break-words text-[13px]">{memo}</span>
</div>
```

- メモ列は`truncate`→`break-words`に変更し、長文でも省略せず折り返して全文表示
- グリッドコンテナは`items-center`→`items-start`に変更し、メモが複数行になっても日付・種別・数量が行の上端で揃うようにした
- `min-w-0`は折り返し（`break-words`）でも列が縮まなくなるのを防ぐために引き続き必要

**実装済み（2026/08/08）：** lintエラーなし（既存2件の警告のみ、CategoryIcon.jsx・ToastContext.jsxのfast-refresh警告で本変更とは無関係）。

## 43. 商品詳細ページ：カテゴリ・単位バッジの横並び化／現在の在庫ボックスの左右レイアウト化

**経緯：** カテゴリ（絵文字＋カテゴリ名）と単位が縦に積まれていて情報密度が低く、また「現在の在庫」ボックスも見出し・数字・発注目安が縦一列に並んでいて視線移動が多かったため、横並びのバッジ／左右レイアウトに変更。

```jsx
// Before
<div className="mb-1 flex items-center gap-1.5">
  <CategoryIcon category={item.category} size={17} />
  <p className="text-[17px] font-bold ...">{item.category}</p>
</div>
<p className="mb-4 text-[15px]" style={{ color: "var(--ink-soft)" }}>単位：{item.unit}</p>

<div className="rounded-[var(--r-lg)] p-5" style={{ background: status.bg }}>
  <p className="text-sm">現在の在庫</p>
  <p className="text-[40px] font-black">{item.stock} <span>{item.unit}</span></p>
  <p className="text-sm">発注目安：{item.threshold}{item.unit}</p>
</div>

// After（43番で確定）
<div className="mb-4 flex flex-wrap items-center gap-2">
  <span className="rounded-full px-3 py-1 text-sm font-bold" style={{ background: "var(--border)", color: "var(--ink)" }}>
    <CategoryIcon category={item.category} size={15} />{item.category}
  </span>
  <span className="rounded-full px-3 py-1 text-sm font-bold" style={{ background: "var(--border)", color: "var(--ink)" }}>
    単位：{item.unit}
  </span>
</div>

<div className="flex flex-col gap-2 rounded-[var(--r-lg)] p-5 sm:flex-row sm:items-center sm:justify-between" style={{ background: status.bg }}>
  <p className="text-sm font-bold">現在の在庫</p>
  <div className="text-right">
    <p className="text-[40px] font-black">{item.stock} <span>{item.unit}</span></p>
    <p className="text-sm mt-1">発注目安：{item.threshold}{item.unit}</p>
  </div>
</div>
```

- カテゴリ・単位バッジは`rounded-full`のミニボックスにし、背景は専用の「薄いニュートラル色」変数が無いため既存の`--border`（oklch(0.9 0.01 260)、`--*-light`系と近い明度のグレー）を流用。`flex-wrap`で狭幅でも折り返し可能
- 「現在の在庫」ボックスは`sm`以上で`flex-row justify-between`（ラベル左・数字＋発注目安を右にtext-rightでまとめる）、`sm`未満は`flex-col`にフォールバックしてラベルを上、数字ブロックを下に積む
- 在庫ステータス別の背景色（`status.bg`）・数字色（在庫切れ赤／在庫少オレンジ／十分グリーン）はそのまま維持

**実装・動作確認済み（2026/08/09）：** Playwrightでデスクトップ（1280px）・モバイル（390px）、在庫十分／在庫少の2パターンを確認。バッジの横並び・折り返し、在庫ボックスの左右レイアウト（モバイルでは縦積み）とも問題なし。lintエラーなし。

## 44. トップページ商品カードのhover明示（22番の分類：画面遷移のみ）

**経緯：** 商品カード（詳細画面への遷移のみ、実データ書き込みなし）にhover時の視覚変化がなかったため、22番の分類上「hover明示のみ」対象として追加。他ボタン（Button.jsxのsecondary: `hover:bg-[var(--border)]! hover:border-[var(--ink-soft)]!`）と同じ考え方で統一。

```jsx
// Before
className="group box-border flex w-full cursor-pointer flex-col items-stretch gap-2 rounded-[var(--r-xl)] border-2 p-3 text-left no-underline"
style={{ background: cardBg, borderColor: cardBorder, color: "inherit" }}

// After（44番で確定）
const hoverClass = status.isOut
  ? "hover:border-[#DC2626]! hover:bg-[#FECACA]!"
  : "hover:border-[var(--ink-soft)]! hover:bg-[var(--border)]!";

className={`group box-border flex w-full cursor-pointer flex-col items-stretch gap-2 rounded-[var(--r-xl)] border-2 p-3 text-left no-underline transition-colors ${hoverClass}`}
style={{ background: cardBg, borderColor: cardBorder, color: "inherit" }}
```

- 通常カードはButtonのsecondary変種と同じ`var(--ink-soft)`（枠線）／`var(--border)`（背景）に統一
- **在庫切れカード（`status.isOut`）は例外的に赤系のまま一段階濃く**する分岐が必要だった：通常カードと同じ`var(--ink-soft)`/`var(--border)`を一律hoverに使うと、在庫切れの赤い枠線・背景がホバー中だけグレーに変わって「在庫切れ」の警告色が消えてしまう見た目のバグをPlaywright検証で発見したため、`status.isOut`時は赤系トークン（`#EF4444`→`#DC2626`、`#FEE2E2`→`#FECACA`）を維持
- border-2・rounded-[var(--r-xl)]など既存スタイルは変更なし

**実装・動作確認済み（2026/08/09）：** Playwrightで通常在庫・在庫少・在庫切れの3状態それぞれhover前後のスクリーンショットを確認。在庫切れカードで警告色が消えないことを確認済み。lintエラーなし。

## 44-1. 商品カードhoverの配色調整＋「一覧へ戻る」・ページネーション数字ボタンへの展開

**経緯：** 44番の通常カードhover（`hover:border-[var(--ink-soft)]! hover:bg-[var(--border)]!`）が濃すぎるという指摘。「枠線変化のみ」案と「`--border`よりさらに薄い背景」案を実際にPlaywrightで見比べ、後者（`var(--bg)`）を採用。枠線変化のみだと変化が分かりにくく、`var(--bg)`案は白い`--surface`背景に対してごく淡いリフト感が出て自然だったため。

```jsx
// Before（44番）
const hoverClass = status.isOut
  ? "hover:border-[#DC2626]! hover:bg-[#FECACA]!"
  : "hover:border-[var(--ink-soft)]! hover:bg-[var(--border)]!";

// After（44-1番で確定：通常カードの背景をより薄く）
const hoverClass = status.isOut
  ? "hover:border-[#DC2626]! hover:bg-[#FECACA]!"
  : "hover:border-[var(--ink-soft)]! hover:bg-[var(--bg)]!";
```

在庫切れカードの赤系分岐（44番）はそのまま維持。

同じ考え方をHeader.jsxの「一覧へ戻る」（商品詳細・新規登録画面などで共通利用）にも展開：

```jsx
// Before
className="inline-flex items-center gap-2 text-[17px] font-bold"

// After
className="-mx-2 -my-1 inline-flex items-center gap-2 rounded-lg px-2 py-1 text-[17px] font-bold transition-colors hover:bg-[var(--bg)]!"
```

枠線を持たないテキストリンクのため、44-1番と同じ`var(--bg)`背景のみで統一。`px-2 py-1`＋`-mx-2 -my-1`で見た目の位置を変えずクリック領域だけ広げている。

また、ページネーション（Pagination.jsx）の数字ボタンには「前へ」「次へ」と同じhoverが付いていなかったため、非アクティブなページ番号ボタンにも同じ`hover:border-[var(--blue)]! hover:bg-[var(--blue-light)]! hover:text-[var(--blue-dark)]!`を適用し一貫性を持たせた（現在ページ＝アクティブ表示中のボタンには適用しない）。

**実装・動作確認済み（2026/08/09）：** Playwrightで商品カード（通常／在庫切れ）・「一覧へ戻る」リンク・ページネーション数字ボタンのhover前後を確認。lintエラーなし。

---

## 44-2. 「一覧へ戻る」hoverを濃く戻す＋残りわずかカードのhoverをオレンジ系に

**経緯：** 44-1番で「一覧へ戻る」リンクのhover背景をカードと同じ`var(--bg)`に揃えたところ、テキストリンク単体では変化が分かりにくく薄すぎるとの指摘。カードは`var(--bg)`のまま、戻るボタンだけ一段濃い`var(--border)`に変更。また、「残りわずか」カードにも通常カードと同じニュートラルなhoverが適用されていたが、在庫切れカードと同様に警告色が保たれるよう、オレンジ系のhoverに変更。

```jsx
// Header.jsx「一覧へ戻る」
// Before（44-1番）
className="-mx-2 -my-1 inline-flex items-center gap-2 rounded-lg px-2 py-1 text-[17px] font-bold transition-colors hover:bg-[var(--bg)]!"

// After（44-2番で確定：カードより一段濃く）
className="-mx-2 -my-1 inline-flex items-center gap-2 rounded-lg px-2 py-1 text-[17px] font-bold transition-colors hover:bg-[var(--border)]!"
```

```jsx
// ItemCard.jsx
// Before（44-1番）
const hoverClass = status.isOut
  ? "hover:border-[#DC2626]! hover:bg-[#FECACA]!"
  : "hover:border-[var(--ink-soft)]! hover:bg-[var(--bg)]!";

// After（44-2番：残りわずかをオレンジ系に分岐）
const hoverClass = status.isOut
  ? "hover:border-[#DC2626]! hover:bg-[#FECACA]!"
  : status.isLow
    ? "hover:border-[var(--orange-dark)]! hover:bg-[var(--orange-light)]!"
    : "hover:border-[var(--ink-soft)]! hover:bg-[var(--bg)]!";
```

残りわずかカードのhoverには、既に在庫状況バッジ・在庫数の文字色として使われている`var(--orange-dark)`（枠線）・`var(--orange-light)`（背景）を再利用し、新規のハードコード色は追加していない。在庫切れカード（isOut）の赤系分岐ロジックはそのまま維持。

**実装・動作確認済み（2026/08/09）：** Playwrightで「一覧へ戻る」リンクの濃さ変化、および残りわずか／在庫切れ／通常カードそれぞれのhover前後を確認。lintエラーなし。

---

## 45. ヘッダー状況フィルター・カテゴリセレクト・検索欄への hover/focus 展開（44番・44-2番の考え方の適用）

**経緯：** 44番・44-2番でカード・戻るボタン・ページネーションに適用したhoverの考え方（ニュートラルは`var(--ink-soft)`枠＋薄い背景、警告色はその色味を保ったまま一段階濃く）を、ヘッダーの状況フィルターボタン・商品の種類セレクトボックス・商品検索入力欄にも展開。

```jsx
// Header.jsx 状況フィルターボタン
// 「すべて」：44番のニュートラルカードと同じ考え方に統一（枠色をblue→ink-softへ）
// Before
className="... hover:border-[var(--blue)]!"
// After
className="... hover:border-[var(--ink-soft)]! hover:bg-[var(--bg)]!"

// 「在庫切れ」：44-2番のisOutカードと同じ色（一段階濃い赤＋薄い赤背景）を再利用
// Before
className="... hover:border-[var(--red)]!"
// After
className="... hover:border-[#DC2626]! hover:bg-[#FECACA]!"

// 「在庫少」：既存の枠hoverはそのまま、背景のみ追加（在庫少バッジのtooltip枠と同じ#FED7AAを再利用）
// Before
className="... hover:border-[var(--orange-dark)]!"
// After
className="... hover:border-[var(--orange-dark)]! hover:bg-[#FED7AA]!"

// 「在庫あり」：既にニュートラル配色（ink-soft）のため、非アクティブ時の背景var(--bg)から
// 一段階濃いvar(--border)へ変化するよう背景hoverのみ追加
// Before
className="... hover:border-[var(--ink-soft)]!"
// After
className="... hover:border-[var(--ink-soft)]! hover:bg-[var(--border)]!"
```

「在庫切れ」「在庫少」は非アクティブ時点で既に`var(--red-light)`/`var(--orange-light)`を背景に使っているため、hover背景をさらに濃い`#FECACA`/`#FED7AA`（既存のバッジtooltip枠色を再利用）にすることで、通常カードの薄い背景hoverと同様に視認できる変化にした。

```jsx
// Home.jsx「商品の種類」セレクトボックス：ニュートラルなhoverを新規追加
// Before
className="box-border min-h-12 cursor-pointer rounded-[var(--r-lg)] border-2 px-4.5 text-base"
// After
className="box-border min-h-12 cursor-pointer rounded-[var(--r-lg)] border-2 px-4.5 text-base transition-colors hover:border-[var(--ink-soft)]!"
```

```jsx
// Home.jsx 商品検索入力欄：focus時の枠色変化を追加
// Before
className="box-border min-h-12 w-full rounded-[var(--r-lg)] border-2 py-3 pr-4 pl-[46px] text-[17px]"
// After
className="box-border min-h-12 w-full rounded-[var(--r-lg)] border-2 py-3 pr-4 pl-[46px] text-[17px] transition-colors focus:border-[var(--blue)]!"
```

検索欄は`index.css`にもともと`input:focus { border-color: var(--blue); }`というグローバル指定があるが、各入力欄のインラインstyleで`borderColor`を固定指定しているため、擬似クラスでは上書きできず実際には機能していなかった。Tailwindの`focus:`＋`!`でインラインstyleを上書きし、既存のグローバル方針（フォーカス＝blue）通りに機能させた。cursorは指定を変更しておらず、標準のI字カーソルのまま。

**実装・動作確認済み（2026/08/09）：** Playwrightで状況フィルター4種のhover前後、セレクトボックスのhover前後、検索欄のfocus前後、および検索欄のcursorスタイル（`text`のまま変化なし）を確認。lintエラーなし。

---

## 45-1. 状況フィルターの選択中ボタンでhoverが白文字と同化する不具合の修正（45番の見直し）

**経緯：** 45番で追加したhoverクラスが選択中（アクティブ）のボタンにも無条件に適用されていたため、選択中ボタン（白文字＋濃い背景）にマウスを乗せると、背景だけが非選択時の薄い色に変化し、白文字はそのまま残ってしまい、文字が背景と同化して読めなくなる不具合が発生していた（「すべて」に限らず「在庫切れ」「在庫少」「在庫あり」も同様の構造で全て同じ不具合を持っていた）。文字色はJSXのinline style側で選択状態によって固定されており、hover用のTailwindクラスからは制御できないため、Pagination.jsx数字ボタン（`p === current`のときはhoverクラスを付与しない）と同じパターンを採用し、選択中のボタンにはhoverクラス自体を適用しないよう修正した。

```jsx
// Before（45番）：選択中でも常にhoverクラスが付与される
className="... transition-colors hover:border-[var(--ink-soft)]! hover:bg-[var(--bg)]!"

// After（45-1番）：選択中のときはhoverクラスを外す
className={`... transition-colors ${activeFilter === "all" ? "" : "hover:border-[var(--ink-soft)]! hover:bg-[var(--bg)]!"}`}
```

「在庫切れ」「在庫少」「在庫あり」の3ボタンにも同じ考え方で`activeFilter === "..."`による条件分岐を適用し、選択中は元の濃い背景＋白文字のまま変化しないようにした。

**実装・動作確認済み（2026/08/09）：** Playwrightで4種のフィルターそれぞれをactiveにした状態で他3ボタン・自分自身のhover前後のbackground/colorを計測し、選択中ボタンはhoverで見た目が変化しないこと、非選択中ボタンは文字色とのコントラストが保たれたまま背景hoverが機能することを確認。lintエラーなし。

---

## 45-2. 選択中ボタンのhoverに枠線のみの変化を追加（45-1番の見直し）

**経緯：** 45-1番で選択中ボタンのhoverを完全に無効化したところ、カーソルが乗っているかどうか分からず不親切という指摘。背景色・文字色（白文字）は変えずコントラストを保ったまま、枠線の色だけを現在の色より一段階濃くすることで、hoverしていることが分かるようにした。非選択ボタンの背景色hover（コントラスト対応済み）はそのまま維持。

```jsx
// Before（45-1番）：選択中はhoverクラスなし
${activeFilter === "all" ? "" : "hover:border-[var(--ink-soft)]! hover:bg-[var(--bg)]!"}

// After（45-2番）：選択中は枠線のみ一段階濃く
${activeFilter === "all" ? "hover:border-[var(--blue-dark)]!" : "hover:border-[var(--ink-soft)]! hover:bg-[var(--bg)]!"}
```

各ボタンの選択中hoverに使った枠線色は、いずれも既存のCSS変数（またはカードhoverで既に使っている色）をそのまま再利用し、新規のハードコード色は追加していない。

| ボタン | 選択中の地色 | 選択中hoverの枠線色 |
|---|---|---|
| すべて | `var(--blue)` | `var(--blue-dark)` |
| 在庫切れ | `var(--red)` | `#DC2626`（44-2番のカードhoverと同色） |
| 在庫少 | `var(--orange)` | `var(--orange-dark)` |
| 在庫あり | `var(--ink-soft)` | `var(--ink)` |

**実装・動作確認済み（2026/08/09）：** Playwrightで4種それぞれをactiveにした状態でhover前後のbackground/color/borderを計測し、background・colorは不変、borderのみ濃い色に変化することを確認。lintエラーなし。

---

## 46. 登録・編集フォーム／入出荷記録画面への focus/hover 展開（45番の考え方の適用）

**経緯：** 45番で商品検索欄・カテゴリセレクトに実装した「focus時に青枠」「hover時にink-soft枠」を、商品登録・編集フォーム（`ItemForm.jsx`）と入出荷記録画面（`StockRecord.jsx`）、写真アップロード枠（`PhotoUpload.jsx`）の全入力要素に展開した。

対象ファイル：`src/pages/ItemForm.jsx`、`src/pages/StockRecord.jsx`、`src/components/PhotoUpload.jsx`

### 1. テキスト・数値入力欄 → focus時に青枠

対象：商品名／カテゴリ「その他」入力／単位／在庫数／発注点（`ItemForm.jsx`）、数量／メモ（`StockRecord.jsx`）

```jsx
// Before（例：商品名）
className={fieldBase}

// After
className={`${fieldBase} transition-colors ${fieldErrors.name ? "" : "focus:border-[var(--blue)]!"}`}
```

商品名・在庫数・発注点は既存のバリデーションエラー（赤枠）と、45番検索欄と同じ理由（インラインstyleのborderColorはグローバルCSSの`input:focus`指定だけでは上書きできない）でTailwindの`focus:`＋`!`が必要。ただし赤枠と青枠が競合するとエラー表示の意味が薄れるため、45-1／45-2番の「アクティブ時はhoverクラスを出し分ける」パターンを踏襲し、`fieldErrors.xxx ? "" : "focus:border-[var(--blue)]!"`という形でエラー中は青枠クラス自体を出さないようにした。単位・カテゴリ「その他」・数量・メモはエラー表示がないため無条件で付与。

### 2. セレクトボックス・写真アップロード枠 → hover時にink-soft枠

```jsx
// Before（カテゴリselect）
className="box-border w-full max-w-[280px] cursor-pointer rounded-[var(--r-md)] border-2 px-4 py-3.5 text-[17px]"

// After
className="box-border w-full max-w-[280px] cursor-pointer rounded-[var(--r-md)] border-2 px-4 py-3.5 text-[17px] transition-colors hover:border-[var(--ink-soft)]!"
```

写真アップロード枠（`PhotoUpload.jsx`）はドラッグ中に枠色をインラインstyleで青に変えているため、フィルターボタンの選択中hoverと同じ考え方で、`dragOver`中はhoverクラスを出し分けて競合を避けた：

```jsx
className={`... transition-colors ${dragOver ? "" : "hover:border-[var(--ink-soft)]!"}`}
```

### 3. ＋/−ボタン → Button.jsxのsecondary hoverと統一

`ItemForm.jsx`・`StockRecord.jsx`の＋/−ボタンは`Button`コンポーネントを使わない独自classNameで組まれていたため、`Button.jsx`のsecondary variantと同じhoverクラスを個別に追加した。

```jsx
// Before
className="h-[52px] w-[52px] shrink-0 cursor-pointer rounded-[var(--r-md)] border-2 text-2xl font-bold"

// After
className="h-[52px] w-[52px] shrink-0 cursor-pointer rounded-[var(--r-md)] border-2 text-2xl font-bold transition-colors hover:border-[var(--ink-soft)]! hover:bg-[var(--border)]!"
```

在庫数・発注点の＋/−（`ItemForm.jsx`）、数量の＋/−（`StockRecord.jsx`）の計6箇所に適用。

**実装・動作確認済み（2026/08/09）：** lintエラーなし（既存の無関係な警告2件のみ）。Playwrightでデスクトップ（1600×1000）・モバイル（390×844）双方、登録フォーム・入出荷記録画面の全対象要素についてfocus/hover前後の`border-color`を計測し、期待通りの変化（青枠／ink-soft枠）を確認。商品名フィールドでバリデーションエラーを発生させた状態でfocusしても、赤枠（`var(--red)`）が青枠に上書きされないことも確認。テキスト入力欄の`cursor`は`text`のまま変化なし。

---

## 47. 入力欄・入荷出荷ボタンの hover/focus 2段階化（46番の見直し）

**経緯：** 46番で追加した「focus時に青枠」だけだと、hoverしているかfocusしているかの区別がつきにくいという指摘。入力欄はhover（枠色のみ変化）→focus（枠色＋リング状box-shadow）の2段階、入荷・出荷ボタンはhover（枠色のみ変化）→選択（既存の色分け）の2段階として、状態の違いをより明確にした。

対象ファイル：`src/pages/ItemForm.jsx`、`src/pages/StockRecord.jsx`

### 1. テキスト・数値入力欄・メモ → hover(1重)→focus(2重)

対象：商品名、単位、在庫数、発注点、カテゴリ「その他」入力欄（`ItemForm.jsx`）／数量、メモ（`StockRecord.jsx`）

```jsx
// Before（46番）
className={`${fieldBase} transition-colors ${fieldErrors.name ? "" : "focus:border-[var(--blue)]!"}`}

// After（47番）
className={`${fieldBase} transition-colors hover:border-[var(--ink-soft)]! ${fieldErrors.name ? "" : "focus:border-[var(--blue)]! focus:shadow-[0_0_0_3px_var(--blue-light)]!"}`}
```

`var(--blue-light)`は既存トークン（`index.css`）にあるため新規追加なしでリング色に流用。hoverクラスは常時付与し、46番のエラー時分岐（エラー中はfocus用クラスを出さない）とは独立させた。この結果、バリデーションエラー中に入力欄をhoverすると枠が一時的にink-softへ変わる（focusクラス自体は出ないため、focusしても赤枠のまま）が、これは今回のhoverクラス独立方針どおりの挙動。

### 2. 入荷・出荷ボタン → hover(枠変化)→選択(色変化)

```jsx
// Before（46番以前）：hoverなし、選択状態のみinline styleで色分け
className={typeBase}
style={type === "in" ? {...green...} : {...neutral...}}

// After（47番）
className={`${typeBase} transition-colors ${type === "in" ? "hover:border-[var(--green-dark)]!" : "hover:border-[var(--ink-soft)]!"}`}
style={type === "in" ? {...green...} : {...neutral...}}
```

未選択時はhoverで枠のみ`var(--ink-soft)`に変化（背景は変えない）。選択中は45-2番と同じ「枠のみ一段階濃く」の考え方で、入荷ボタンは`var(--green-dark)`（既存の選択中文字色と同じ値を再利用）、出荷ボタンは`var(--blue-dark)`（同様に既存の選択中文字色を再利用）を使用し、背景・文字色は変えていない。

**実装・動作確認済み（2026/08/09）：** lintエラーなし（既存の無関係な警告2件のみ）。Playwrightでデスクトップ（1600×1000）・モバイル（390×844）双方、各入力欄でrest→hover→focusの順に`border-color`・`box-shadow`を計測し、hoverでink-soft枠、focusで青枠＋`0 0 0 3px var(--blue-light)`のリングが付くことを確認。入荷・出荷ボタンは選択中・未選択どちらの状態でもhover前後で背景色が不変であることを確認。テキスト入力欄の`cursor`は`text`のまま変化なし。

---

## 48. トップページ「商品を探す」欄への hover/focus 2段階化展開（47番の考え方の適用）

**経緯：** 47番で登録・編集フォーム／入出荷記録画面の入力欄に実装した「hover(1重)→focus(2重リング)」を、トップページの商品検索欄（「商品を探す」）にも展開し、サイト全体でテキスト入力欄のhover/focus挙動を統一した。

対象ファイル：`src/pages/Home.jsx`

```jsx
// Before（45番時点：focusのみ）
className="box-border min-h-12 w-full rounded-[var(--r-lg)] border-2 py-3 pr-4 pl-[46px] text-[17px] transition-colors focus:border-[var(--blue)]!"

// After（48番：47番と同じhover→focusの2段階に統一）
className="box-border min-h-12 w-full rounded-[var(--r-lg)] border-2 py-3 pr-4 pl-[46px] text-[17px] transition-colors hover:border-[var(--ink-soft)]! focus:border-[var(--blue)]! focus:shadow-[0_0_0_3px_var(--blue-light)]!"
```

47番と同じトークン・数値（`var(--ink-soft)`、`var(--blue)`、`0 0 0 3px var(--blue-light)`）をそのまま再利用し、新規追加はなし。この検索欄にはバリデーションエラー表示がないため、47番のようなエラー時分岐は不要でhover/focusクラスとも無条件付与。

**実装・動作確認済み（2026/08/09）：** lintエラーなし（既存の無関係な警告2件のみ）。Playwrightでデスクトップ（1600×1000）・モバイル（390×844）双方、rest→hover→focusの順に`border-color`・`box-shadow`を計測し、hoverでink-soft枠、focusで青枠＋`0 0 0 3px var(--blue-light)`のリングが付くことを確認。`cursor`は`text`のまま変化なし。

---

## 49. 商品カード「›」の専用ボックス廃止→在庫数行末への統合（12-3の明示を保ちつつ高さ圧縮）

**経緯：** トップページ商品カード（`ItemCard.jsx`）のカード最下部にあった横幅いっぱいの「›」専用ボックス（`min-h-[48px]`、背景・枠線・角丸あり）が、カード内で場所を取りすぎているという指摘。12-3番で決めた「›」表示自体（カードがクリック可能であることの明示）は維持しつつ、専用ボックスを廃止して在庫数表示の行末に小さく統合し、カード全体の高さを圧縮した。

対象ファイル：`src/components/ItemCard.jsx`

```jsx
// Before
<p className="m-0 flex items-baseline gap-1.5">
  <span className="text-[11px] font-medium" style={{ color: "var(--ink-soft)" }}>在庫</span>
  <span className="text-[32px] font-black" style={{ color: ... }}>{item.stock}</span>
  <span className="text-sm font-semibold" style={{ color: ... }}>{item.unit}</span>
</p>

<span
  aria-hidden="true"
  className="box-border flex min-h-[48px] items-center justify-center rounded-[var(--r-md)] border-2 text-xl font-bold"
  style={{ background: "var(--surface)", color: "var(--ink)", borderColor: "var(--border)" }}
>
  <svg>...chevronアイコン...</svg>
</span>

// After
<p className="m-0 flex items-center justify-between gap-1.5">
  <span className="flex items-baseline gap-1.5">
    <span className="text-[11px] font-medium" style={{ color: "var(--ink-soft)" }}>在庫</span>
    <span className="text-[32px] font-black" style={{ color: ... }}>{item.stock}</span>
    <span className="text-sm font-semibold" style={{ color: ... }}>{item.unit}</span>
  </span>
  <span aria-hidden="true" className="shrink-0" style={{ color: "var(--ink-soft)" }}>
    <svg>...同じchevronアイコン...</svg>
  </span>
</p>
```

専用ボックス（背景・枠線・角丸・`min-h-[48px]`）は完全に削除し、在庫数表示の`<p>`を`justify-between`にして行末にchevronを配置。色は既存の枠線色（`var(--border)`）より一段濃い`var(--ink)`から、目立ちすぎない`var(--ink-soft)`に変更。`aria-hidden="true"`は維持し、実際のクリック領域は18-21番の方針どおりカード全体の`<a>`のまま変更していない。商品名の右隣にある在庫少／在庫切れの警告アイコン（⚠️/✕）とは別の行（在庫数行）に配置されるため、警告アイコンとの視覚的な混同はない。

**実装・動作確認済み（2026/08/09）：** lintエラーなし（既存の無関係な警告2件のみ）。Playwrightでデスクトップ（170px幅グリッド）・iPad Mini（768px）・モバイル（390px）の3サイズでカード高さを計測し、いずれも227.5px→171.5px（56px圧縮）を確認。3桁在庫（例：`999本`）でも在庫数・単位・chevronが同じ行に収まり、折り返し・はみ出しは発生しないことを確認。カードクリック時に商品詳細ページへ正しく遷移する（クリック領域に変更がない）ことも確認。

---

## 50. 商品グリッドの行数を「画面高さ」から自動計算（31-5番の考え方を縦方向にも拡張）

**経緯：** 31-5番でResizeObserverを使い、CSSグリッドの実際の`gridTemplateColumns`から列数を実測している仕組みを、縦方向（1ページに表示する行数）にも拡張した。これまで1ページの行数は`3`固定だったため、画面が大きい端末では下に余白が余り、小さい端末では逆にスクロールが必要になっていた。

対象ファイル：`src/components/ItemList.jsx`

### 計算ロジック

```jsx
// カード1枚の高さ（49番で171.5pxに圧縮済み）とグリッドのgap-4（16px）
const CARD_HEIGHT = 171.5;
const GRID_GAP = 16;
const ROW_HEIGHT = CARD_HEIGHT + GRID_GAP;
// 固定ページネーションバーの実測高さ（py-3 24px + min-h-[44px] + border-t-2 2px = 70px）＋ gap-4ぶんの余白
const BOTTOM_RESERVE = 70 + GRID_GAP;

const measure = () => {
  // 列数（31-5番と同じロジック）
  const tracks = getComputedStyle(gridEl).gridTemplateColumns.split(" ").filter(Boolean);
  setColumns(Math.max(1, tracks.length));

  // 行数（今回追加）
  const availableHeight = window.innerHeight - gridEl.getBoundingClientRect().top - BOTTOM_RESERVE;
  const rawRows = Math.floor(availableHeight / ROW_HEIGHT);
  setRows(Math.max(3, Math.min(4, rawRows)));
};
```

`pageSize = columns * rows`として1ページの表示件数を決定し、`totalPages = Math.ceil(items.length / pageSize)`でページ数を再計算する。

当初は下余白の基準にHome.jsxの`pb-36`（144px）をそのまま流用していたが、実際に固定表示される`Pagination`バーの高さを実測したところ70pxだったため、`70px + gap-4(16px)`＝86pxに変更した（`pb-36`はスクロール時の余白を広めに取るための値で、行数計算の基準としては過大だったため）。

### リサイズ・ページ調整

既存の列数測定用`ResizeObserver`に相乗りしつつ、グリッド自身の横幅が変わらず縦方向（ウィンドウの高さ）だけが変化するケースも拾えるよう、`window`の`resize`イベントリスナーも追加した。

現在ページの調整は、既存の`const currentPage = Math.min(page, totalPages)`（レンダー毎に導出される値）がそのまま機能するため、追加コードは不要だった。リサイズで`totalPages`が現在の`page`より小さくなった場合は自動的に最終ページへクランプされ、`totalPages`が再び増えれば元々選択していたページ番号に戻る。

### 4サイズでの実測結果

| 端末（想定） | ビューポート | 実測列数×行数 | 目安との比較 |
|---|---|---|---|
| MacBook Air | 1440×900 | 7列×3行 | 目安「7列×3〜4行」の範囲内 |
| iPad Pro 11（横向き） | 1194×834 | 5列×3行 | 目安「5列×4行」よりやや少（画面が低いぶん行数が1減） |
| iPad Air（縦向き） | 820×1180 | 3列×4行 | 目安どおり一致 |
| iPad mini（縦向き） | 768×1024 | 3列×3行 | 目安どおり一致 |

**実装・動作確認済み（2026/08/09）：** lintエラーなし（既存の無関係な警告2件のみ）。Playwrightで上記4サイズの実測列数・行数・ページネーション件数を確認。ウィンドウをリアルタイムでリサイズ（例：1440×900 → 820×1180）した際に列数・行数・総ページ数が追従することを確認。2ページ目に移動した状態で幅を狭めて総ページ数が5ページに増える→さらに5ページ目へ移動→ウィンドウを1440×900に戻して総ページ数が2ページに減少、という操作でも、現在ページが自動的に最終ページ（2ページ目）へ調整されることを確認。端数（最終行が列数に満たない場合）でもカードの縮小・詰め込みは発生しないことを確認。

---

## 50-1. 実機回転（縦→横）での列数・行数の自動追従を確認（50番の見直し）

**経緯：** 50番の4サイズ実測で、iPad Proのみ横向き（1194×834）、iPad Air・iPad miniは縦向き（820×1180／768×1024）というように、端末ごとにテストした向きが揃っていなかった（目安の数値に近い向きをそれぞれ選んで測定していたため）。同じ端末を縦→横に回転させた場合に列数・行数が自動で入れ替わることを確認できれば、向き専用の分岐コードが不要であることの証明になるという指摘を受け、実際に回転させて再検証した。

`src`配下を`orientation`／`landscape`／`portrait`で検索した結果、該当コードは0件。50番の列数・行数計算は`window.innerHeight`と実際の`gridTemplateColumns`の実測値のみに依存しており、向き固有の分岐は元々存在しない。

### 回転による実測結果（同一端末・縦→横→縦）

| 端末 | 縦向き（列×行） | 横向き（列×行） | 縦向きに戻す |
|---|---|---|---|
| iPad Pro 11 | 4列×4行（834×1194） | 5列×3行（1194×834） | 4列×4行（元に戻る） |
| iPad Air | 3列×4行（820×1180） | 5列×3行（1180×820） | 3列×4行（元に戻る） |
| iPad mini | 3列×3行（768×1024） | 5列×3行（1024×768） | 3列×3行（元に戻る） |

いずれも回転で列数・行数が即座に入れ替わり、逆回転で元の値に戻ることを確認（状態の残留・ズレなし）。50番で報告した「iPad Pro 11（横向き）5列×3行」は、この回転結果の横向き値と一致しており、報告内容自体に誤りはなかった。

**実装・動作確認済み（2026/08/09）：** Playwrightのビューポート変更（幅・高さの入れ替え）による回転シミュレーションで、上記3端末それぞれ縦→横→縦の3状態を確認。コード変更なし（50番の実装がそのまま向き非依存で機能することの追加検証）。

※ 本環境には実機・iOS Simulatorへのアクセスがないため、この検証はPlaywrightのビューポートサイズ入れ替え（実際の`resize`イベントが発火し、ブラウザの回転時と同じDOM再計算が起きる）によるものであり、ハードウェアの回転ジェスチャーそのものでの確認ではない。向き固有のコードが存在しない以上は実質的に同等の検証と考えられるが、正確を期すため明記する。

---

## 50-2. iPad以外（汎用Androidタブレット想定サイズ）での動作確認

**経緯：** 50番・50-1番の検証はいずれもiPad系サイズのみだったため、「この仕組みはiPad専用ではないか」という疑問が挙がった。50番の計算ロジックが`window.innerHeight`・`gridTemplateColumns`の実測値のみに依存し、端末ブランドや`orientation`／`landscape`／`portrait`等のUA判定を一切参照していないことは50-1番で確認済みだが、実際にiPad以外の画面サイズでも同じ結果になるかを実測して裏付けた。

一般的なAndroidタブレットの解像度として広く使われる**1280×800**（例：Nexus 10相当のアスペクト比）を使用。

| 画面サイズ | 列×行 | 1ページ件数 | ページ数（317件） | 検証 |
|---|---|---|---|---|
| 1280×800（横） | 6列×3行 | 18件 | 18ページ | 317÷18≒17.6→18 ✓（実測も18） |
| 800×1280（縦） | 3列×4行 | 12件 | 27ページ | 317÷12≒26.4→27 ✓（実測も27） |

横幅1280pxはiPad Pro横（1194px）とiPad Air横（1180px）よりやや広く、列数（6列）もその中間的な結果になった。縦向き（800px）の列数はiPad mini縦（768px）に近く、同じ3列という結果になっている。いずれも「端末名」ではなく「実際の画面サイズ」に比例した結果であることが確認できた。

なお、事前に用意していた下書きでは縦向き（800×1280）を「3列×3行・9件・36ページ」としていたが、実測したところ「3列×**4行**・12件・27ページ」だった。800×1280は50-1番のiPad Air縦（820×1180）より高さが100px大きく、rows計算（`Math.max(3, Math.min(4, rawRows))`）の上限である4行に達するため、3行ではなく4行が正しい。ページ数についても、表示されているページ番号ボタンの最大値だけでなく「次へ」を実際に押して到達できる最終ページまで数える方法で二重に確認し、317÷12を切り上げた27ページと一致することを確認した。

**結論：** この仕組みは端末のブランド（Apple／Android／Windows等）に依存せず、ブラウザが報告する画面サイズだけで一貫して動作する。iPad以外のタブレット、将来のモバイル対応（将来スコープ）でも、追加の分岐コードなしで同じロジックがそのまま機能する見込み。

**実装・動作確認済み（2026/08/09）：** Playwrightのビューポートサイズ指定（1280×800／800×1280）と317件のテストデータで、実測列数・行数・1ページ件数を確認。総ページ数は表示中のページ番号ボタンの最大値と、「次へ」を連打して実際に到達した最終ページ数の両方で突き合わせて確認。コード変更なし（50番の実装が端末ブランド非依存で機能することの追加検証）。

※ 50-1番と同様、本環境には実機のAndroidタブレット・Windowsタブレットへのアクセスがないため、この検証はブラウザの検証モード（DevToolsレスポンシブモード／Playwrightのビューポート指定）によるものであり、実機での確認ではない。計算ロジックがUA判定や端末固有コードを持たないことは50-1番でコード検索により確認済みのため、挙動自体は実機でも同様になる見込みだが、実機での最終確認は9番の持ち越しタスク（Windows実機確認）と合わせて行うことが望ましい。

---

## 51. 店舗情報の保存機能：ヘッダーバッジへの即時反映漏れを修正

**経緯：** 店舗情報編集画面（`src/pages/Shop.jsx`）・`useShop`フック・`services/localStorage.js`は、保存先キー`inventory_shop`（商品データの`inventory_items`・`inventory_logs`と同じ命名規則。単一オブジェクトのため配列ではなくオブジェクトとして保存）への読み書きも含めてすでに実装済みだった。デフォルト値（キー未設定時）も`{ name: "田中商店", address: "", phone: "" }`で、保存タイミングも「保存する」ボタン押下時のみ（オートセーブなし）と、想定していた方針にすでに合致していた。

そのため今回は実装ではなく動作確認を行ったところ、1点不具合を確認した。

**発見した不具合：** 店舗情報編集画面で店舗名を変更・保存した直後、トップページへ遷移してもヘッダーの「🏪 店舗名 ›」バッジが古い名前のままだった（ページを再読み込みしないと反映されない）。

原因は`Header.jsx`が`useShop()`フックを使っており、そのフック内部の`useEffect(() => setShop(loadShop()), [])`が空配列依存でマウント時の1回しか実行されないため。同じ`Header.jsx`内の商品件数（`items`/`logs`）は`loadItems()`/`loadLogs()`を`location.pathname`変化のたびに直接呼び直す実装にすでになっており、店舗名だけこのパターンから外れていた。

**対応：** `Header.jsx`側で`useShop`フックの利用をやめ、商品件数と同じ「画面遷移のたびに`loadShop()`を呼び直す」パターンに統一した（`Shop.jsx`側の`useShop`フック自体は変更なし）。

```jsx
// Before（Header.jsx）
const { shop } = useShop();
useEffect(() => {
  setItems(loadItems());
  setLogs(loadLogs());
}, [location.pathname]);

// After（Header.jsx）
const [shop, setShop] = useState(loadShop());
useEffect(() => {
  setItems(loadItems());
  setLogs(loadLogs());
  setShop(loadShop());
}, [location.pathname]);
```

**確認事項：**
- 既存のバリデーション・hover/focus実装（46〜47番）：`Shop.jsx`は変更していないため影響なし
- Playwrightで一連の流れを確認：①キー未設定時に「田中商店」がデフォルト表示 → ②編集画面のフォーム初期値も「田中商店」→ ③店舗名・住所・電話番号を変更して保存 → ④`localStorage`の`inventory_shop`に正しい値が書き込まれている → ⑤遷移直後にヘッダーバッジが新しい店舗名に即時反映（修正前はここが失敗していた）→ ⑥ページ再読み込み後もヘッダー表示が保持 → ⑦編集画面に戻ってもフォームに保存済みの値が保持

**実装・動作確認済み（2026/08/09）：** 保存機能自体は実装済みだったため、`Header.jsx`のヘッダー即時反映漏れのみ修正。Playwrightでキー未設定→保存→画面遷移→リロード→再編集の一連の流れを確認し、いずれも想定どおりの値になることを確認。

---

## 52. favicon・ヘッダーロゴのデザイン確定（一本書き家アイコン）

**経緯：** Viteデフォルトのfaviconと`Header.jsx`の旧ロゴ（線画の「箱＋屋根線」アイコン、煙突なし）が、実際のアプリロゴ・ブランドイメージと不一致だったため、Canvaで生成したアイコン案をベースに、家アイコンをオリジナルのSVGパスとして確定した。

### 経緯のポイント

- Canva生成のアイコン画像（ラスター）は「形はそのまま活かしたいが、ラスターのままではSVG化できない」という制約があったため、画像の白い線画部分だけをマスク抽出し、`potrace`でベクタートレースする手法で、見た目を変えずに正確なSVGパスへ変換した。
- favicon（実際のブラウザタブ表示・16px）とヘッダーロゴ（アプリ内表示・46px前後）は、視認性の要件が異なるため、**あえて別デザインとして管理する**方針とした。
  - favicon：16pxでの視認性を最優先し、線を太らせて再トレースし、塗りつぶしではなく「白背景＋濃い青の太い一本書き」に変更（線画のまま最大限のコントラストを確保）
  - ヘッダーロゴ：46px前後というある程度の大きさで表示されるため、より繊細な「グラデーション背景＋白い細い一本書き」を採用

### favicon（`public/favicon.svg`）

- 背景：白（`#ffffff`）、角丸`rx=20`相当
- アイコン：濃い青（`--blue-dark`相当）の太い一本書き家シルエット（煙突付き）、塗りつぶし
- 枠に対して約80%のサイズで中央配置
- 線を太くした理由：16pxのブラウザタブサイズでは、細い線画は潰れて視認できなくなるため。元画像をいったん膨張処理（線を太く）してから再度ベクタートレースすることで、デザインの「一本書き」感を保ったまま視認性を確保した

### ヘッダーロゴ（`Header.jsx`）

```jsx
// Before（旧ロゴ：線画の「箱＋屋根線」、煙突なし）
<div
  className="flex h-[46px] w-[46px] shrink-0 items-center justify-center rounded-[var(--r-md)]"
  style={{ background: "var(--blue)" }}
>
  <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
    <rect x="4" y="9" width="16" height="11" rx="2" stroke="white" strokeWidth="2" />
    <path d="M4 9L12 4L20 9" stroke="white" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
  </svg>
</div>

// After（52番で確定：一本書き・煙突付きの家シルエット、グラデーション背景）
<div
  className="flex h-[46px] w-[46px] shrink-0 items-center justify-center rounded-[var(--r-md)]"
  style={{ background: "linear-gradient(180deg, color-mix(in srgb, var(--blue-light) 50%, var(--blue) 50%), var(--blue-dark))" }}
>
  <svg width="39" height="35" viewBox="0 0 236.5 211" fill="none">
    <path
      fill="white"
      d="M1235 2188 ... (一本書き家アイコンのパスデータ) ... 218 -170z"
      transform="translate(-10.5,220) scale(0.1,-0.1)"
    />
  </svg>
</div>
```

- 角丸：`var(--r-md)`のまま変更なし（他のUI要素との統一性を優先）
- アイコンサイズ：コンテナ（46px）に対して約85%（旧デザインの52%から拡大）
- **背景グラデーションの調整過程（試行錯誤の記録）：**
  1. `var(--blue)` → `var(--blue-dark)`：家マークを大きくした直後に確認したところ「強すぎる／デカすぎる」という印象。原因を色のコントラストと判断。
  2. `var(--blue-light)` → `var(--blue)`：明るい側に振ったところ、屋根・煙突部分（グラデーション最上部＝最も薄い箇所）で白いアイコン線がほぼ視認できなくなる問題が発生。
  3. `color-mix(in srgb, var(--blue-light) 50%, var(--blue) 50%)` → `var(--blue-dark)`（**確定**）：1と2の中間の濃さを、CSSの`color-mix()`で`--blue-light`と`--blue`を50%ずつ混合した色として上端に採用。下端は`--blue-dark`のまま。屋根・煙突部分のコントラストを保ちつつ、下方向にほんのり深みを持たせるバランスに着地。
- 新規のハードコード色は追加せず、既存トークン（`--blue-light`・`--blue`・`--blue-dark`）の組み合わせのみで表現。

**実装・動作確認済み（2026/08/09）：** favicon・ヘッダーロゴともにブラウザ実機で表示確認。ヘッダーロゴは屋根・煙突部分を含め白線のコントラストが十分に保たれていることを確認。lintエラーなし（既存の無関係な警告2件のみ）。コミット`94d398b`でpush済み。

### 店舗アイコン（🏪）について（今回の検討・据え置き判断）

- Getty Images等の店舗アイコン画像を参考として検討したが、以下の理由で**現行の🏪絵文字を維持**する判断とした。
  - 12-5番で確定している「カテゴリ絵文字アイコン」方式（文房具✏️・雑貨🎁・食品🍙など）との統一性を優先
  - 店舗アイコンだけを一本書き線画のオリジナルデザインに変更すると、カテゴリ絵文字群とデザインの毛色が変わり、アプリ全体でちぐはぐな印象になるリスクがある
  - 9/1の発表スケジュールを踏まえ、追加のデザイン作業を最小限に抑える判断
- 将来的にアプリ全体のアイコンを一本書き線画スタイルに統一する場合は、カテゴリアイコン全体を含めた大きめの改修として、別途スコープを切って検討する（今回はスコープ外）。

---

## 未決定・次回検討事項

- [ ] 上記をTailwindの共通クラス（例：`btn-primary`, `btn-danger` など）としてコンポーネント化するか
- [x] `categoryStyle.js` の絵文字アイコンを、将来的にはSVGアイコンセットに置き換えるか → 41番で絵文字方式を正式維持と決定（一部SVGへの先行実装は誤りと判明し、絵文字に復元済み、2026/08/08）
- [ ] 店舗情報画面のデータ保存先（LocalStorageの既存Itemデータとは別キーで保存する想定）
- [ ] 権限構造（9番）は実装スコープ外だが、Itemデータモデルに影響が出ないか、実装フェーズで再確認する
- [x] トップページの最終チェック（チャッピーによるレビュー）完了。design-tokens 18-11〜19番に反映済み
- [x] 18番の改修内容を商品詳細画面・商品登録フォームにも展開（22番・23番・24〜26番として実装・確認済み、2026/08/03）
- [x] 商品詳細画面まわり（22番・23番・14-2追記）完了（2026/08/03）
- [x] 登録フォームのバリデーションエラー表示ルール決定・実装・確認済み（24番、2026/08/03）
- [x] 在庫数・発注点の入力方法を直接入力＋ステッパー併用に変更・実装・確認済み（25番、2026/08/03）
- [x] 登録フォームのキャンセル時データリセット・写真デフォルト表示の不具合修正済み（26番、2026/08/03）
- [x] ページネーションの番号付き拡張・配置（中央寄せ）・固定表示、実装・確認済み（27番、2026/08/03）
- [x] 数字入力欄の全角対応、実装・確認済み（28番、2026/08/03）
- [x] 入出荷記録画面の数量入力ボックス幅調整、実装・確認済み（29番、2026/08/03）
- [x] 固定ヘッダー（「戻る」系リンク）のsticky top化・重なり不具合修正、実装・確認済み（30番、2026/08/03）。商品詳細・登録フォーム・入出荷記録・店舗情報など全画面で同じ見た目になっていることを確認済み
- [x] 入出荷記録画面・店舗情報画面へのhover明示・処理中表示の展開（22番の「遷移のみ vs 実処理あり」分類基準を適用、40番、2026/08/08、コミット`f2023ba`）
- [ ] 訓練校のWindowsデスクトップ機での列数・見た目・色表示の実機確認（次回最優先。Macとは論理解像度の仕組みが異なるため、13-2の見積もりがそのまま当てはまらない可能性あり）
- [ ] 25番の直接入力欄化に伴い、他の画面（入出荷記録・店舗情報など）でも同様の全角対応・先頭ゼロ正規化が正しく機能しているか横展開確認
- [ ] 27番（ページネーション）について、実際の商品データで9ページ超・省略記号ありのケースを実機でも確認する
- [x] トップページ商品カードの再圧縮（170px固定）・ステータス表示のアイコン化・ツールチップ改善・「›」統一・ページネーション列数バグ修正、実装・確認済み（31番、2026/08/04）
- [x] Canva連携の試行結果（画面全体は不採用、カテゴリアイコン試作は保留）を記録（32番、2026/08/06）
- [x] PC幅2カラムレイアウト化（登録フォーム・商品詳細ページ）、実装・確認済み（33番、2026/08/08）
- [x] 登録・編集フォーム細部調整（数字ボックス拡幅・単位欄縮小・カテゴリ8種類化・コンテナ幅960px化等）、実装・確認済み（34番、2026/08/08）
- [x] 「戻る」リンクの/items/undefinedバグ修正（Header.jsxのuseParams問題）、実装・確認済み（35番、2026/08/08）
- [x] 在庫数・発注点ボックスの重なりバグ修正（flex-1→flex-auto）、実装・確認済み（36番、2026/08/08）
- [x] 登録・編集フォーム 2カラムレイアウトの仕上げ調整（在庫数・発注点の全幅化、gap縮小、ボタン幅拡大、表示順変更、カテゴリ・単位統合、ボタン右端揃え等）、実装・確認済み（37番、2026/08/08）
- [ ] 「その他」カテゴリカードの見分けにくさへの対応（31-7、優先度低・保留）
- [ ] 現在の状況パネルの件数バッジをクリック可能にする案（31-7、18-11の方針変更が必要なため保留）
- [x] 33番・37番の変更内容がGitHub mainブランチに正しく反映されているか確認 → 反映済みと確認（コミット`ca17e87`）。当初の「未反映では」という懸念はraw.githubusercontent.comのキャッシュによる誤検知だった（2026/08/08）
- [x] チャッピーレビュー反映：写真枠縮小・発注点文言修正・保存ボタン文言変更（38番）、実装・確認済み（コミット`c98e762`、2026/08/08）
- [x] 39番（タブレット幅バランス調整：コンテナ幅md:対応・ボタンgap拡大・写真ブロック中央寄せ）、実装・4サイズでの確認済み（2026/08/08）
- [x] 39-4：39番実装後、iPad Proの2カラム表示の窮屈さは解消されたため、2カラム化基準（`lg:`1024px）の変更は不要と判断（2026/08/08）
- [x] 39-5：白いカード下のページ余白について、画面高さに比例する一般的なWeb挙動と判断し、対応不要（選択肢A採用、2026/08/08）
- [ ] 企画書7章「発注点未満の商品を赤色表示」→「発注点以下の商品を赤色表示」への文言修正
