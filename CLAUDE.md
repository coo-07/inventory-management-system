# CLAUDE.md

このファイルは、このリポジトリで作業する際にClaude Codeが従うべきルールをまとめたものです。

## プロジェクト概要

React 19 + Vite + react-router-dom 7 + Tailwind CSS 4で構築した在庫管理アプリ（`inventory-app`）。

- `npm run dev` — 開発サーバー起動
- `npm run build` — 本番ビルド
- `npm run lint` — oxlintによるlint
- `npm run preview` — ビルド結果のプレビュー

UIの実装方針・変更履歴は `design-tokens.md` にまとめています。

## コミットメッセージ

- 必ず日本語で記述すること（英語は使用しない）
- 「〜を追加」「〜を修正」のような簡潔な体言止め・過去形で書く
